# Crazyflie 2.1 Brushless 多机协同吊运（悬挂刚体负载）仿真

> ## ✅ 当前状态（重要，请先读这一段）
>
> 本目录的 6 个 `.m` 文件已按 Lee 2014 / 2018 的**多机协同吊运**框架实现：
> **3 架**四旋翼各自通过一根**绳索**吊在同一刚体负载的不同挂点上，
> 控制器包含论文最核心的**张力分配**（分配矩阵 `P` 的伪逆）。
> 可通过 `cfg.vehicle.count` 配置机数（要求 `n >= 3`，见 §5 的说明）。
>
> ### 默认工况：绕八字避障（对应论文 Fig. 3 算例 / Fig. 2 快照）
>
> 直接运行 `crazyflie_slung_demo` 得到的就是论文 Lee 2018 §V 的数值算例：
> 三架机吊着负载沿**八字曲线绕开两个锥形障碍物**，并且加了用户要求的
> **起飞段**与**降落段**：
>
> ```text
> 阶段 0  起飞    t ∈ [0, 3)     竖直爬升到环绕平面
> 阶段 1  环绕    t ∈ [3, 21)    绕两个完整八字，横宽 ±1.5 m，绕开两个锥
> 阶段 2  降落    t ∈ [21, 25]   水平归位 + 竖直下降到落点
> ```
>
> 三段在拼接点上**位置 / 速度 / 加速度全部连续（C²）**。
>
> ### 八字工况实测结果（最终参数集，Python 镜像全程 25 s）
>
> ```text
> 起飞段 [0, 3.0 s]      : 跟踪误差峰值      23.4 mm
> 环绕段 [3.0, 21.0 s]   : 峰值 686.6 mm / 均值 133.7 mm
> 降落段 [21.0, 25.0 s]  : 峰值             125.4 mm
> 降落终点残差           :                  82.0 mm
> 轨迹行程 (x, y)        : (3.076, 1.709) m    （期望 x 幅值 ±1.50 m，y 峰谷差 1.62 m）
> 到锥形障碍物最小净间隙 :                 210.2 mm   → PASS（判据 > 50 mm）
>   自检有效性            : [有效] 锥高度区间内采样 23582 点（旧版此处静默失效，见 §10.3.1）
>   锥尖离地 / 环绕平面   : 1.05 m / 0.55 m（锥尖更高 ⇒ 避障是真实约束）
> 绳索长度不变量         : 最大偏差 2.22e-16 m → PASS（判据 < 1e-9 m）
> 稳态负载姿态误差       : roll/pitch 0.001 deg（可控轴，完全收敛）
> 稳态绳向误差           : 1.018 deg
> 张力范围 / 最小裕度    : 0.0985 ~ 0.4172 N（37.6 %，绳全程绷紧）
> 推力峰值占比           : 70.8 %
> 最大机体角速度         : 1.005 rad/s
> 状态有限 / 各机在负载上方: True / True
> ```
>
> **★ 复现这个结果需要两个非默认改动（缺一不可）**，原因见 §10：
> 1. `cfg.figureEight.blendTime = 6.0`（不是 1.2）—— 消除包络加速度尖峰；
> 2. `USE_QID_DOT = false`（在 `crazyflie_slung_controller.m` 里）——
>    去掉 `q_id_dot` 的差分数值噪声。
>
> ### 静态悬停工况（对照）
>
> 清空 `cfg.referenceFcn = []` 可退回静态悬停。该工况下 16 项自检全部通过，
> 关键数字（Python 镜像 30 s，稳态窗口取后 20 %）：
>
> ```text
> 负载终位置        : [-1.50e-04, 2.38e-03, -3.47262e-01] m
>                     （目标 [0,0,-0.35]，误差 3.63 mm）
> 稳态位置误差      : 4.24 mm
> 稳态各绳索张力    : [0.2620, 0.2620, 0.2619] N   （理论 m0 g/n = 0.2616 N）
> 推力峰值占比      : 87.3 %
> 最大机体角速度    : 1.876 rad/s
> 稳态 roll/pitch   : 0.0005 deg   （可控轴：完全收敛）
> 稳态 yaw          : 14.14 deg    （欠驱动轴：自由漂移）
> ```
>
> **已逐项验证正确的部分**：
> - **动力学**：6×6 降阶形式在精确配平点上严格成立 —— `x0_ddot = 0`、
>   `Omega0_dot = 0`、`q_ddot = 0`（机器精度），并以论文 (18) 为判据
>   交叉验证（残差 2.3e-5 N，另一个候选写法为 2.4e-2 N，差三个数量级）；
> - **张力分配**：`P · diag[R0'] · mu = [R0' Fd; Md]` 精确成立，
>   三个力矩通道的增益均为 1.0（机器精度）；`cond(P P') = 276.87`，
>   满足论文 (13) 的 `rank(P) >= 6`；
> - **解析悬停解**：每根绳索张力 `mu_i = m0 g / n = 0.2616 N`，
>   每机推力 `f_i = m0 g / n + m_i g = 0.5804 N`（43.5% 上限），
>   三机完全对称；
> - 静态检查（函数可见性 / cfg 字段路径 / 结构体字段 / 块配平 / 先用后定义）
>   全部 **0 问题**（`_verify_python/_lint_matlab.py`）。
>
> **★ 必须知道的一条结论：负载偏航通道必须关闭**
>
> `cfg.loadController.kR(3) = kOmega(3) = 0`。这不是调参偷懒，而是经过
> 系统辨识确认的**结构性结论**：本构型（近似竖直绳索 + 水平面内挂点）下，
> 负载的偏航轴**不可控**，一旦给偏航通道任何非零增益，闭环必然发散
> （已扫描过 1/50、1/77 名义增益、`kO0[3] = 0.002` 等所有取值，
> 全部发散）。完整机理见 §5.1 与 §9.1 修复 3。
>
> **已知的物理代价**：偏航角是自由积分轴，会缓慢漂移。静态工况实测到 60 s 时
> 偏航角速度达到约 1.0 rad/s（另两个分量仅 1e-3 量级），完整发散出现在
> **77.2 s**。八字工况 25 s 内漂移率仅 0.061 rad/s，无影响。
> 工程解法：用抗扭绳索，或加第 4 架机。详细趋势见 §6.2 / §6.3。

在 MATLAB R2021b 下运行的多机吊运仿真系统。控制器以 Lee 2014/2018 论文的
**推力 + 力矩**方案为蓝本，改造为 **推力 + 角速度指令**方案，使仿真输出
可以直接对应 `cflib` 的 `send_setpoint_manual(..., rate=True)` 接口。
物理参数采用 **Crazyflie 2.1 Brushless** 的官方规格。

---

## 1. 文件职责

```text
crazyflie_slung_parameters.m     物理参数、控制增益、初始条件、可视化配置
crazyflie_slung_dynamics.m       完整动力学：负载平动/转动 + 连杆摆 + 机体姿态
crazyflie_slung_controller.m     几何外环：负载外环、张力反解、摆方向环、姿态外环
crazyflie_slung_simulation.m     主程序：积分、执行器模型、日志、性能指标
crazyflie_slung_visualization.m  结果曲线 + 三维动画
crazyflie_slung_demo.m           一键运行与结果自检脚本
```

依赖：**仅 MATLAB 基础功能**，不需要 Simulink、Aerospace Toolbox 或
Robotics System Toolbox。

### 1.1 重要的代码组织约定（修改代码前必读）

MATLAB 有一条容易踩的规则：**写在某个 `.m` 文件末尾的局部函数是"文件私有"的**，
只有同一个文件内部的代码才能调用它，其它文件既看不见也调用不到。

因此本项目刻意让 `normalizeVector` / `projectSO3` / `expSO3` / `hat` / `vee` /
`clamp` / `clampVector` 这些工具函数在**需要它们的每个文件里各自重复定义一份**：

| 文件 | 各自定义的局部工具函数 |
|---|---|
| `crazyflie_slung_parameters.m` | `rpyToRotm`, `normalizeVector`, `mergeStruct` |
| `crazyflie_slung_dynamics.m` | `normalizeVector`, `hat`, `projectSO3` |
| `crazyflie_slung_controller.m` | `normalizeVector`, `vee`, `hat`, `projectSO3`, `so3Log`, `so3ProjectedRate`, `clamp`, `clampVector` |
| `crazyflie_slung_simulation.m` | `normalizeVector`, `projectSO3`, `expSO3`, `hat`, `clamp`, `clampVector` 及日志辅助函数 |
| `crazyflie_slung_visualization.m` | `payloadBoxVertices` 等绘图辅助 |
| `crazyflie_slung_demo.m` | `emptyCheckList`, `addCheck` |

这是 MATLAB 的标准做法，**不是冗余代码**，也与 `v2` 文件夹中现有代码的组织方式一致。
若图省事改成跨文件调用（例如让 `simulation.m` 直接调用 `dynamics.m` 里的
`normalizeVector`），运行时就会报 `函数或变量 'xxx' 无法识别`。

> **修改工具函数时请同步修改各文件中的同名副本**，否则会出现"某个文件的
> `normalizeVector` 改了、其它文件的没改"的不一致。
> `_verify_python/_lint_matlab.py` 可以自动检测这类跨文件可见性问题（见 §9.2）。


## 2. 运行

```matlab
% 完整演示（画图 + 三维动画）
crazyflie_slung_demo

% 只要数值结果（无图形环境）
cfg = struct();
cfg.visualization.plot = false;
cfg.visualization.animate = false;
sim = crazyflie_slung_simulation(cfg);
disp(sim.summary)
```

## 3. 坐标系与符号约定

完全沿用论文约定，**不要**与常见的“z 轴向上”混用：

| 符号 | 含义 | 代码字段 |
|---|---|---|
| `e3 = [0;0;1]` | 指向**重力方向**，z 轴**向下为正** | — |
| `x0` | 负载质心位置 | `state.loadPosition` |
| `R0` | 负载姿态，体系 → 惯性系 | `state.loadRotation` |
| `q` | 连杆单位向量，**由四旋翼指向负载** | `state.linkUnit` |
| `R` | 机体姿态，体系 → 惯性系 | `state.rotation` |
| `f` | 总推力幅值，推力向量为 `-f R e3` | — |

由刚性连杆约束，四旋翼位置由负载位姿和连杆方向反解：

```text
x = x0 + R0 * rho - l * q
```

`f > 0` 时推力指向 `-b3`，即机体上方，所以**负载悬挂在四旋翼下方**，
`q` 大致指向 `+e3`（向下）。

绘图时纵轴显示为 `height = -z`，便于阅读。

## 4. 控制方案

### 4.1 与论文的对应关系

| 论文公式 | 内容 | 本实现位置 |
|---|---|---|
| (6)-(9) | 完整动力学，含负载–连杆–机体耦合 | `crazyflie_slung_dynamics.m` |
| (20)-(24) | 负载位置/姿态误差、期望合力 `Fd`、期望力矩 `Md` | 控制器第 (1) 层 |
| (25)-(26) | `P` 伪逆分配张力，单机时 `mu_d = Fd` | 控制器第 (2) 层 |
| (16)-(19) | `u_parallel` 的耦合补偿，由 `mu` 反解 | 控制器第 (3) 层 |
| (27)-(28) | 张力沿杆投影、期望连杆方向 `q_d = -mu_d/‖mu_d‖` | 控制器第 (3) 层 |
| (30)-(33) | 摆方向环，得到 `u_perp` | 控制器第 (4) 层 |
| (36)-(39) | 期望姿态 `Rc`、推力 `f = -u' R e3` | 控制器第 (5) 层 |
| **(40)** | **原文：力矩 `M`** | **改为角速度指令 `Omega_cmd`** |
| (2018) 第 IV 节 | 积分项 / 扰动补偿 | 负载环与摆环的积分器 |

### 4.2 关键改造：力矩环 → 角速度环

论文 (40) 式的力矩控制器为

```text
M = -kR/eps^2 * e_R - kOmega/eps * e_Omega + Omega x J Omega
    - J (Omega_hat R' Rc Omega_c - R' Rc Omega_c_dot)
```

论文用奇异摄动证明 `eps -> 0` 时姿态快速收敛到 `Rc`，从而整机控制力
`-f R e3 -> u`。本实现把这一环替换为**期望角速度**：

```text
Omega_cmd = R' Rc Omega_c  -  kR_att .* e_R  -  kOmega_att .* e_OmegaR
```

物理含义：内层速率环负责产生力矩，外层只指定“该以多快的角速度转过去”。
`e_R` 项相当于对期望姿态做 P 控制，`e_OmegaR` 项提供阻尼，`R' Rc Omega_c`
是期望姿态自身的改变速率前馈。

**代价与对策**

- 角速度环的力矩是有限带宽产生的（本仿真用 `rateLoop.bandwidth` 一阶等效，
  对应 Crazyflie 固件约 30–50 rad/s 的速率环带宽），因此姿态收敛不再是
  “瞬时”的，原论文的奇异摄动假设被削弱；
- 必须对角速度指令限幅 `maxBodyRateCommand`，并对姿态误差限幅
  `maxAttitudeError`，否则初始大偏差会产生超出物理能力的翻转指令；
- 姿态外环增益 `attitudeController.kR/kOmega` 需明显低于速率环带宽，
  本参数取 `kR = [10;10;5]`、`kOmega = [4;4;4]` rad/s，约为速率环带宽的 1/4，
  保证级联稳定；
- 数值扫描显示，在 `kq = 55`（摆环已足够快）之后，姿态增益从 4 提到 14
  对结果**几乎没有影响**，说明瓶颈不在姿态环，无需在此处过度调参。

### 4.3 级联结构

```text
负载位姿 (x0, R0)
      |-- 负载位置外环 -> Fd
      |-- 负载姿态外环 -> Md
      v
张力分配 mu_d = Fd ,  q_d = -mu_d/||mu_d||
      v
u_parallel = mu_d + ml||omega||^2 q + m Q R0 hat(Omega0)^2 rho
      |
摆方向环 (30)-(33) -> u_perp
      v
总控制力 u = u_parallel + u_perp
      |---------------------------|
      v                           v
推力 f = -u'Re3              期望姿态 Rc
      v                           v
一阶推力执行器              角速度外环 -> Omega_cmd
      |                           v
      |                    速率环（等效固件） -> M
      |---------------------------|
                  v
        完整动力学 (6)-(9) 积分
```

### 4.4 实现中的三处关键修正（重要）

论文的方程在搬运到代码时有三个极易写错、且错了以后"能跑但结果不对"的地方。
这三处都经过数值验证（悬停平衡校验 + 与论文 (34) 式逐点比对）。

#### (1) 等效质量矩阵 `Mq` 必须是 `m0 I + sum_i m_i q_i q_i'`（**不是** `(m0 + sum m_i) I`）

论文 (6) 的负载平动方程左端是 `Mq (x0_ddot - g e3)`，其中

```text
Mq = m0 I + sum_i m_i q_i q_i'
```

**这里有一个非常反直觉的点**：`Mq` 的**单位矩阵系数只有 `m0`**，
不是总质量 `m0 + sum m_i`。机体的质量是通过 `sum_i m_i q_i q_i'`
这个**方向相关**的项进来的，而不是各向同性的单位项。

三机情形：

```text
Mq = m0 I + sum_{i=1..3} m_i q_i q_i'
```

悬停时所有 `q_i ≈ e3`，于是 `sum m_i q_i q_i' ≈ (sum m_i) e3 e3'`，
`Mq` 在**竖直方向**确实等于总质量、在**水平方向**只有 `m0`。
写成 `(m0 + sum m_i) I` 会让水平方向的等效惯量凭空多出三架机的质量，
水平响应变慢，与竖直方向不一致。

**为什么容易错**：论文 (17) 又写了一个看似等价的
`m0 (x0_ddot - g e3) = sum_i mu_i`，容易被误读成"总质量就是 m0"。
两者是不同的方程 —— (17) 是**负载单独的**受力平衡（右端只有张力，
不含机体推力），(6) 是**整个耦合系统**的平动方程。本实现按 (6) 求解，
所以 `Fd` 里用的是 `m0`（见 §4.1 的控制器结构），
但 `Mq` 里必须带 `sum_i m_i q_i q_i'` 这一项。

**另外一个陷阱**：`m0 I` 中的 `I` 是 3×3 单位矩阵，
`sum_i m_i q_i q_i'` 是 3×3 的秩 1 矩阵，**两项都要有**，
漏掉前者会让 `Mq` 在 `q_i` 张成的方向之外完全退化（奇异）。

**验证方法**：令所有 `q_i = e3`、`Omega0 = 0`，要求 `x0_ddot = 0`：

```matlab
% 代入 (6)：  Mq (x0_ddot - g e3) = u_parallel
% 要求 x0_ddot = 0
%  => u_parallel = -Mq g e3 = -(m0 + sum m_i) g e3   （悬停时竖直方向确为总质量）
% 取 n = 3, m0 = 0.080, m_i = 0.0325:
u = -(0.080 + 3*0.0325) * 9.81;      % = -1.741 N
% 回代动力学，应得到 x0_ddot = q_ddot = Omega0_dot = 0（机器精度）
```

注意这个悬停校验在**竖直方向**上 `Mq e3 = (m0 + sum m_i) e3` 成立，
所以看起来"总质量是对的" —— 但把同一个 `Mq` 拿去算水平运动就会露出破绽。
**必须用扰动方向（如 `x0_ddot` 含水平分量）才能检出这个错误。**

#### (2) 耦合系统 (2,1) 块中必须有 `Q`

把 (6) 代入 (7) 消去 `x0_ddot` 后得到的 6×6 系统为

```text
[ Mq                , -m Q R0 hat(rho) ] [ x0_ddot    ]   [ RHS_x ]
[ m hat(rho)' R0' Q ,  J0 + D          ] [ Omega0_dot ] = [ RHS_w ]
```

左下块的 `Q = q q'` 来自 (7) 中的 `sum_i m_i hat(rho_i) R0' q_i q_i' (x0_ddot - g e3)`。
**漏掉 `Q` 不会报错**（维度仍合法），但会错误地改变平动与转动的耦合强度。

#### (3) 摆环 `u_perp` 的大括号符号

论文 (33) 的原文形式是

```text
u_perp = -m l hat(q) { kq e_q + kw e_w + ... } - m hat(q)^2 a
```

大括号内是**正号**，但因为整体前置了负号，代入摆方程 (8) 后得到论文 (34) 的
闭环 `omega_dot = -kq e_q - kw e_w - ...`，即负反馈。

**如果漏掉整体负号**（写成 `correction = +kq*eq + kw*ew`），负反馈变成正反馈，
摆角指数发散。这是本控制器最致命的一处笔误。

**验证方法**（已用 200 组随机状态与论文 (34) 逐点比对至 1e-9）：

```matlab
% 取 q = e1, qd = e3, omega = 0, a_i = 0, kq = 55
% 期望 omega_dot 第一分量 = -kq = -55（负）
% 若得到 +55，则大括号符号写反了
```

代码中对应写法：

```matlab
correction = -cfg.linkController.kq * eqi ...
    - cfg.linkController.komega * eOmegaI ...
    - cfg.linkController.kqIntegral * memory.linkIntegral ...
    - dot(q, omegaD) * qdDot ...
    - hatQSq * omegaDDot;
uPerp = m * l * cross(q, correction) - m * hatQSq * aApprox;
```

#### (4) 附带的两个数值陷阱

- **`hat(v)^2 * w` 的优先级**：`hat(q)^2 * a` 在 MATLAB 中虽是合法的矩阵运算，
  但与其它矩阵乘法混写时极易因优先级产生维度错误的表达式（例如被解析为
  `hat(q) * (hat(q)^2 * a)`）。代码中统一抽成 `hatQSq = hat(q) * hat(q)`
  中间变量，语义明确。
- **初始 `linkUnit` 方向**：`q` 定义为"由四旋翼指向负载"，悬停时 `q ≈ +e3`，
  **不是** `-e3`。写成 `-e3` 会让初始构型变成"负载在四旋翼上方"的倒挂状态。

## 5. 参数：Crazyflie 2.1 Brushless

| 参数 | 数值 | 来源 / 说明 |
|---|---|---|
| 质量 `m` | 0.0325 kg | Bitcraze 官方规格（含电池，不含桨叶保护罩） |
| 最大总推力 | 1.3344 N | 4 × 34 gf = 136 gf |
| 单电机最大推力 | 0.3336 N | 34 gf |
| 惯量 `J` | `diag(2.395e-5, 2.395e-5, 3.234e-5)` kg·m² | 按 0.0325 × 0.0325 × 0.04 m 均质长方体估算 |
| 机臂长度 | 0.0465 m | 电机轴到质心 |
| 推力时间常数 | 0.012 s | 电机 + 桨叶一阶近似 |
| 速率环带宽 | 35 / 35 / 20 rad/s | 固件速率环的等效一阶带宽 |

> **惯量说明**：Bitcraze 未公开 Brushless 版本的转动惯量。表中数值由长方体模型
> 估算，属于同量级估计；真实飞行前必须通过台架辨识替换。质量与推力为官方数据。

### 负载与连杆（3 机协同）

| 参数 | 数值 | 说明 |
|---|---|---|
| 机数 `n` | **3** | 论文 (13) 的 `rank(P) >= 6` 要求 `n >= 3` |
| 负载质量 `m0` | 0.080 kg | 三机协同，负载可比单机版重 |
| 负载惯量 `J0` | `diag(2.6933e-4, 2.6933e-4, 5.3333e-4)` kg·m² | 长方体公式 |
| 挂点半径 | 0.085 m，方位角 `[90, 210, 330]°`，`rho_z = -0.010` m | 绕质心均布，3 次对称 |
| 连杆长度 `l` | 0.35 m | 摆自然频率 `sqrt(g/l) = 5.294 rad/s` |
| 单机质量 `m_i` | 0.0325 kg | — |
| 单机最大推力 | 1.3344 N | 4 × 34 gf |

**解析悬停解**（论文 (17)(18) 的闭式解，已用镜像核对到机器精度）：

```text
每根连杆张力   mu_i = -m0 g / n            = -0.2616 N   （负号：连杆向上拉负载）
负载总受力     n * mu_i = -m0 g            = -0.7848 N   ← 平衡负载重力
每机推力       f_i  = m0 g/n + m_i g       =  0.5804 N   ← 43.5 % 上限
```

> **注意 `mu_i` 是负数**。`mu_i` 是连杆作用在负载上的力，悬停时必须向上
> （对抗重力），而本项目的 `e3` 指向**下**，所以 `mu_i < 0`。
> 与之对应，`q_i`（由机指向负载）≈ `+e3`，负载挂在机**下方**。
> 这是自洽的，不要被负号误导成"符号写错"。
>
> **`Fd` 只用 `m0`，不用总质量**。论文 (23) 的期望合力是
> `Fd = m0(...)`，因为 `Mq = m0 I + sum m_i q_i q_i'` 里的
> `sum m_i q_i q_i'` 项在悬停时全部指向 `e3`，两侧的机身重量已经在
> 张力分配里被 `mu_i` 之外的机体推力承担。用 `(m0 + sum m_i)` 代替 `m0`
> 会让负载整体上浮。

### 5.1 ★ 负载偏航通道为什么必须关闭（本节是 3 机版本最关键的发现）

#### 现象

3 机版本的第一次实现完全无法收敛：稳态位置误差 97.6 m、张力 −125.7 ~ 217.2 N、
推力 100% 饱和。逐个排查后确认**动力学与张力分配都是精确正确的**，
问题出在负载姿态外环的第 3（偏航）通道：

| 试验 | 终止时刻 | 终止位置误差 | 收敛 |
|---|---|---|---|
| 全开（基准） | 0.79 s | 7.09e+03 | ✘ |
| **只关 yaw**（`kR0[3]=kO0[3]=0`） | 16.00 s | **1.98e-03** | **✔** |
| 只关 roll/pitch | 0.43 s | 5.16e+03 | ✘ |
| 只留 roll | 16.00 s | 2.11e-03 | ✔ |
| 只留 pitch | 16.00 s | 1.61e-03 | ✔ |
| 只留 yaw | 0.43 s | 5.16e+03 | ✘ |
| 全关（对照） | 16.00 s | 2.71e-03 | ✔ |

**只有偏航通道是毒药**：单独保留 roll 或 pitch 都能收敛，
单独保留 yaw 则立刻发散。

#### 机理（四步链）

论文 (13) 的 `rank(P) >= 6` 只保证**张力分配方程有解**，
并不保证**每个姿态通道都可控**。本构型正好在偏航轴上失效：

1. **`Md_z` 只能产生纯水平的张力扰动。** `hat(rho_i)` 的竖直列系数是
   `rho_{i,z} = −0.010`，水平列系数是 `rho_{i,x}, rho_{i,y} ≈ 0.085`，
   比值 8.5 倍。解 `[sum mu; sum hat(rho_i) R0' mu] = [0; e_z]` 得到的
   `mu_id` 的竖直分量实测**恰好为 0**。
2. **`q_id` 被甩向水平。** 由 (28) `q_id = −mu_id/‖mu_id‖`，
   单位大小的 `Md_z` 会让 `q_id` 偏转 **86°**（接近水平）。
3. **连杆方向环跟不上。** 连杆环带宽 `sqrt(kq) = 7.42 rad/s`
   （`kq = 55`），而期望方向在横摆，于是 (27) 的投影
   `mu_i = q_i q_i' mu_id` **把水平分量整个丢掉**。
   实测力矩交付率：
   ```text
   期望力矩 [0.022, 0.010, 0.086] N·m
   实际交付 [0.022, 0,     0    ] N·m      → 交付率 24.9 %
   提高增益后                               → 交付率 0.0000 %
   q_id 被甩到 [-0.968, 0, 0.250]（几乎水平）
   ```
4. **净效果是纯正反馈。** 偏航环既**没有正作用**（交付力矩为 0），
   又**只有副作用**（把 `q_id` 污染成水平方向，破坏另两个通道）。
   实测等效增益 `M_eff_z = +0.617 N·m`，与输入**同号**，
   环路增益 `0.617 / 0.258 = 2.39 > 1`。

#### 为什么"改增益"救不回来

做过但不成功的尝试（全部记录在 `_verify_python/` 的诊断脚本里）：

| 尝试 | 结果 |
|---|---|
| `kR0[3]` 降到 1/50 名义值 | 仍发散 |
| `kR0[3]` 降到 1/77 名义值 | 仍发散 |
| `kO0[3] = 0.002` | 仍发散 |
| 从 `q_id` 方向指令里去掉 `Mz` | 仍发散 |
| 限幅 `mu_id` 扰动量（`beta` = 0.05 … 1.2） | 仍发散 |
| 把偏航阻尼绕道"张力幅值" | 仍发散 |
| `kR0[3]` 提到 0.6168 以上 | 仍发散 |

**任何一个非零的偏航增益，在任何幅值下都发散** —— 这证明它是结构性缺陷，
不是调参问题。

#### 处置

```matlab
% crazyflie_slung_parameters.m
wnLoad   = 2 * pi * [6.0; 6.0; 0.0];   % 第 3 项 = 0 表示关闭偏航通道
zetaLoad = 0.90;
kRLoad     = diag(payloadInertia) .* wnLoad.^2;        % → [0.382778; 0.382778; 0]
kOmegaLoad = 2 * zetaLoad * wnLoad .* diag(payloadInertia);  % → [0.0182763; 0.0182763; 0]
cfg.loadController.yawChannelEnabled = false;          % 显式标记，防止后人误改
```

> **注意**：`kR`/`kOmega` 必须**乘上 `J0` 的分量**来配置，因为
> `wn = sqrt(kR/J0)`、`zeta = kOmega/(2 sqrt(kR*J0))`。
> 负载惯量 `J0 ≈ 2.7e-4` kg·m²，量级很小，忘记乘 `J0` 会让增益差好几个数量级。
> 另外挂点的 `z = −0.010` 也必须非零——若把挂点放到同一水平面上
> （`rho_{i,z} = 0`），`P` 的秩会直接掉到 5，分配方程无解。

### 增益整定依据

增益不是拍出来的，而是先按带宽配置、再用数值扫描校正。系统的特征频率
必须形成时间尺度分离：

```text
速率环带宽     35 / 35 / 20 rad/s
摆自然频率     sqrt(g/l) = sqrt(9.81/0.35) = 5.294 rad/s
连杆方向环     sqrt(kq)  = sqrt(55)      = 7.42 rad/s  （1.40 × 摆自然频率）
负载水平回路   sqrt(kx)  = sqrt(3.0)     = 1.73 rad/s
负载姿态回路   6.0 Hz = 37.7 rad/s（roll/pitch 两轴，yaw 关闭）
```

**摆回路**：带宽必须高于被控对象的自然频率，否则控制器比对象还慢，摆角会
发散到 `q -> -e3`（负载翻到四旋翼上方）。取

```text
kq     = 55   ->  omega_n = sqrt(55)          = 7.42 rad/s = 1.40 × omega_p
komega = 20.0 ->  zeta    = komega/(2*omega_n) = 1.35   （略过阻尼）
```

> ★ **3 机情形下 `kq = 55` 已是稳定上限**，`kq = 70` 就开始发散（见 §9.1 修复 6）。
> 这与单机版"`kq` 越大越好"的结论不同，原因写在 §9.1。

**负载回路**：取 `kx = [3.0; 3.0; 3.75]`（水平 `omega_n = 1.73 rad/s`），
`kv = [3.12; 3.12; 3.12]`（`zeta ≈ 0.9`），垂向略取大值。

#### 最终增益一览（3 机版，已通过 16/16 自检）

| 组 | 字段 | 值 | 设计依据 |
|---|---|---|---|
| `loadController` | `kx` | `[3.0; 3.0; 3.75]` | 水平 `omega_n = 1.73 rad/s` |
| | `kv` | `[3.12; 3.12; 3.12]` | `zeta ≈ 0.9` |
| | **`ki`** | **`[1.60; 1.60; 1.60]`** | 扫描单调改善，无副作用（§9.1 修复 6a） |
| | `kR` | `[0.382778; 0.382778; **0**]` | `J0 .* (2*pi*6)^2`，**yaw = 0** |
| | `kOmega` | `[0.0182763; 0.0182763; **0**]` | `2*0.9*wn*J0`，**yaw = 0** |
| | `c1` | `0.50` | 积分器交叉项系数 |
| | `integralLimit` | `[0.50; 0.50; 0.50]` | 抗积分饱和 |
| `linkController` | **`kq`** | **`55.0`** | **稳定上限**，70 即发散 |
| | `komega` | `20.0` | `zeta = 1.35` |
| | `kqIntegral` | `0.0` | 实测开了变差（§9.1 修复 6c） |
| `attitudeController` | `kR` | `[30; 30; 15]` | 约速率环带宽的 1/4 |
| | `kOmega` | `[4; 4; 4]` | 阻尼 |
| | `maxBodyRateCommand` | `[5; 5; 3.5]` | 指令限幅 |
| | `maxAttitudeError` | `2.0` | 误差范数限幅 |
| `rateLoop` | `bandwidth` | `[35; 35; 20]` | 固件速率环等效带宽 |

#### 调参记录（实测对照）

**单机版阶段**（保留作历史，`m0 = 0.020`）：

| 配置 | 稳态位置误差 | 推力峰值 | 说明 |
|---|---|---|---|
| 初版 `m0=0.030, kx=1.2, ki=0.06, kq=6` | 87 mm | 100 % | `Mq` 误用 + 摆回路过慢 + 积分不足 |
| `ki = 0.10` | 33 mm | — | 积分不足 |
| `kx = 4.5` | — | 100 % | 推力饱和，反而更差 |
| `kq = 90 / 120` | — | 86 % | 连杆误差反而变差（1.13° / 1.58°） |
| 单机最终 `ki=1.60, kR=[30,30,15], kq=55` | 1.09 mm | 85.8 % | — |

**3 机版阶段**（`m0 = 0.080`，本实现的最终标定）：

| 配置 | 稳态位置误差 | 连杆误差 | 推力峰值 | 说明 |
|---|---|---|---|---|
| 偏航通道全开 | — | — | 100 % | **完全发散**（修复 3） |
| 关闭偏航后 + 旧增益 `ki=0.60, kq=55` | 15.95 mm | 1.812° | 87.3 % | 收敛，但裕度偏紧 |
| `ki=1.20` | 6.30 mm | 2.004° | 87.3 % | `ki` 有效 |
| **`ki=1.60, kq=55, kqIntegral=0`** | **4.24 mm** | **2.009°** | **87.3 %** | **本实现采用（16/16 通过）** |
| `kq = 70` | — | — | — | **发散于 9.2 s** |
| `kqIntegral = 1.5` | 24.02 mm | 1.420° | 87.3 % | 反而不如 0 |

> **调参要点（3 机版）**：
> - `ki` 是稳态精度的主控，而且是**安全维**（只改善位置，不动连杆与推力）；
> - `kq` 是**危险维**，55 已是上限，改大立刻发散（与单机版直觉相反）；
> - `kqIntegral` **不要开**；
> - 最关键的调参动作其实是**关闭偏航通道**，它把系统从"完全发散"
>   变成"可调"（见 §5.1）。

## 6. 仿真结果（3 机协同）

默认工况：负载从初始位置收敛到目标悬停点 `[0, 0, -0.35]` m
（`z` 向下为正，即离地 0.35 m），三架机的初始机体姿态各有偏差，
负载初始姿态偏离水平。三根连杆初始均近似竖直。

### 6.1 数值结果（`ki = 1.60` 最终参数集）

| 指标 | 实测值 | 评价 |
|---|---|---|
| 稳态位置误差（24–30 s 窗口均值） | **4.24 mm** | 远优于 2 cm 目标 |
| 负载终位置误差 | **3.63 mm** | `[-1.5e-4, 2.4e-3, -0.347262]` vs 目标 `[0,0,-0.35]` |
| 稳态连杆误差 | 2.009° | 见 §6.2 —— 受偏航漂移缓慢注入，**非定值** |
| 稳态 roll/pitch 姿态误差 | **0.0005°** | 可控轴：完全收敛 |
| 稳态偏航姿态误差 | 14.1° | 欠驱动轴：自由漂移 |
| 稳态各连杆张力 | `[0.2620, 0.2620, 0.2619] N`（理论 0.2616 N） | 三机均分，偏差 0.15 % |
| 张力范围 | 0.1809 – 0.5206 N | **始终为正**，连杆未松弛 |
| 推力峰值 | **87.3 %** | 保留 12.7 % 裕度 |
| 最大机体角速度 | 1.876 rad/s | 有界 |
| 偏航漂移率（30 s 时刻） | 0.606 rad/s | 见 §6.3 |
| 三机是否始终在负载上方 | **是** | 构型正确 |
| 状态有限性 | **是** | 30 s 内无数值发散 |

> **`ki` 从 0.60 提到 1.60 的效果**（`_scan_ki_3drone.py` 实测）：
> 稳态位置误差从 15.95 mm 降到 **4.24 mm**，而连杆误差、推力峰值
> **完全不变**（±0.2 %）。原因是 `ki` 只作用于负载平动通道，
> 不与摆环/姿态环争夺权限 —— 这是 3 机情形下调参最干净的一维。

镜像输出原文（30 s，`ki = 1.60`）：

```text
负载终位置        : [-1.5400e-04  2.3760e-03 -3.4726e-01]
                     (目标 [0 0 -0.35], 误差 3.63 mm)
稳态位置误差      : 4.24 mm
稳态连杆误差      : 2.0089 deg
稳态负载姿态误差  : [roll pitch yaw] = [ 0.0002  0.0005  14.1416] deg
   -> roll/pitch 范数 = 0.00047 deg
   -> yaw（欠驱动轴）  = 14.142 deg
稳态各连杆张力    : [0.2620 0.2620 0.2619] N
理论张力 m0 g/n   : 0.2616 N
张力范围          : 0.1809 ~ 0.5206 N
推力峰值占比      : 87.3 %
最大机体角速度    : 1.876 rad/s
负载角速度终值    : z 分量 = 0.606 rad/s（偏航漂移率）
状态有限          : True
各机始终在负载上方: True
```

### 6.2 收敛进程与"稳态"的真实含义（★ 必读）

#### 逐点趋势（`ki = 1.60`，80 s 仿真）

| 时段 | 位置误差 | 连杆误差 | roll/pitch 误差 | 偏航误差 | 推力峰值 |
|---|---|---|---|---|---|
| 0 s | 808 mm | 4.357° | 3.60° | 4.05° | 83.9 % |
| 5 s | 89.9 mm | 0.794° | 0.005° | 15.5° | — |
| 10 s | 39.5 mm | 0.927° | 0.006° | 21.1° | — |
| 15 s | 17.6 mm | 1.123° | 0.009° | 3.9° | — |
| 20 s | 8.25 mm | 1.418° | 0.014° | −42.7° | — |
| 25 s | 4.58 mm | 1.821° | 0.017° | −31.7° | — |
| **30 s** | **3.63 mm** | 2.319° | 0.015° | 51.7° | **87.3 %** |
| 35 s | 3.96 mm | 2.850° | 0.038° | −43.0° | — |
| 40 s | 4.61 mm | 3.224° | 0.024° | −3.9° | — |
| 50 s | 6.28 mm | 4.525° | 0.028° | 32.1° | — |
| 60 s | 9.02 mm | 6.428° | 0.024° | 36.0° | — |
| 70 s | 12.3 mm | 9.488° | 0.127° | −55.3° | — |
| 75 s | 67.1 mm | 18.79° | 0.277° | −7.1° | — |
| **77.2 s** | — | — | — | — | **发散** |

#### 三条必须说清楚的结论

**1. 位置误差在 t ≈ 30 s 达到最小值 3.63 mm，之后缓慢回升。**
它不是单调收敛到某个常值 —— 30 s 恰好是最优点，这也是默认演示时长取 30 s
的直接原因。

**2. 连杆方向误差**不是收敛到常值，而是**以约 +0.057 °/s 的速率单调增长**
（`_check_longterm_3drone.py` 用 10–30 s 线性拟合测得）。增长机理是一条明确的链：

```text
负载偏航角漂移  ->  挂点方位角 rho_i 随 R0 转动
                ->  分配矩阵 P 的有效几何改变，期望张力分布改变
                ->  连杆方向环带宽只有 7.42 rad/s，追赶不及
                ->  留下不断增大的连杆方向残差
                ->  残差通过 (27) 的投影消耗一部分张力交付能力
                ->  位置误差随之缓慢回升
```

**3. 这是"欠驱动轴"的必然代价，不是控制缺陷。**
roll/pitch 两个可控轴的稳态残差是 **0.005 – 0.28 deg**（数值零），
偏航是 ±55 deg 过零振荡且持续漂移。**把三轴混在一起看会得出错误结论**
（"姿态环没整定好"），必须按轴分开判读（见 §9.5）。

#### 对演示时长的建议

| 时长 | 位置误差 | 连杆误差 | 评价 |
|---|---|---|---|
| 10 s | 39.5 mm | 0.93° | 尚未收敛 |
| 20 s | 8.25 mm | 1.42° | 接近最优 |
| **30 s** | **3.63 mm** | **2.32°** | **推荐：位置精度最优，连杆误差仍可接受** |
| 40 s | 4.61 mm | 3.22° | 位置开始回升 |
| 60 s | 9.02 mm | 6.43° | 连杆误差偏大 |
| 77 s | — | — | 发散 |

`cfg.simulation.duration` 默认 **30 s** 正是基于此表 —— 它同时是位置误差的
最小点。若要更长的演示，必须先解决偏航漂移（见 §6.3），否则连杆方向会
持续恶化直至发散。

### 6.3 偏航漂移的量级（诚实说明）

因为偏航通道被关闭，负载偏航角是一个**自由积分轴**：

```text
t =  30 s : 偏航角速度 ≈ 0.61 rad/s，偏航角误差 ≈ ±52 deg（过零振荡）
t =  60 s : 偏航角速度 ≈ 1.0  rad/s
t = 77.2 s : 出现完整发散（偏航角持续转，挂点方位角随之旋转，
            连杆方向残差累积到 19 deg，最终张力交付失效）
```

这是**已被完全理解、且符合理论预期的行为**，不是缺陷：
被控对象在该轴上没有可用作动器（见 §5.1 的四步链）。
工程上的三种解法，按代价排序：

1. **抗扭连杆**（推荐）：用碳纤维方管或加一对平行连杆，
   直接提供偏航反扭矩，`rank(P)` 的第 6 个方向被激活；
2. **加第 4 架机**：`n = 4` 时 `P` 是 6×12，冗余度更高，
   偏航通道的可控性显著改善（论文 2014 版也建议 `n >= 3`，4 机更稳）；
3. **加偏航角反馈到挂点方位**：把负载偏航角作为状态反馈进分配矩阵，
   但需要重新推导 `P(rho_i(R0))` 的时变形式，属于研究级改动。

> **对演示的影响**：30 s 的默认演示窗口内，偏航角速度只有 0.6 rad/s，
> 位置误差降到 **3.63 mm**，连杆误差 2.32°，负载在动画里的缓慢自转可见但不影响
> 三机协同的直观效果。

### 6.4 自检脚本（16 项断言）

`crazyflie_slung_demo.m` 内置 **16 项**结果合理性断言。运行后会打印逐项
PASS/FAIL 与关键指标：

```matlab
crazyflie_slung_demo          % 完整运行（30 s）+ 自检 + 绘图 + 三维动画
crazyflie_slung_demo("quick") % 只自检，不绘图（12 s）
report = crazyflie_slung_demo % 取回 report.allPassed 与 report.checks
```

16 项断言及其判据（第 16 项为绳索长度不变量，见 §9.7）：

| # | 断言 | 判据 | 实测 |
|---|---|---|---|
| 1 | 分配矩阵 `rank(P) = 6` | `= 6` | 6 |
| 2 | 张力分配 `sum(mu_id) = Fd` | 残差 `< 1e-9` | 机器精度 |
| 3 | 张力分配 `sum(rho_i × mu_id) = Md` | 残差 `< 1e-9` | 机器精度 |
| 4 | 状态量有限（无 NaN/Inf） | `true` | true |
| 5 | 各绳方向 `q_i → +e3` | `< 15°` | 2.01° |
| 6 | 每架四旋翼都在负载上方 | `true` | true |
| 7 | 所有绳索张力恒为正（单边约束 `mu >= 0`） | `min > 0` | 0.1809 N（裕度 69.2 %） |
| 8 | 稳态张力 `≈ m0 g/n` | 偏差 `< 20%` | 0.15 % |
| 9 | 稳态位置误差 | `< 2 cm` | **4.24 mm** |
| 10 | 稳态绳向误差 | `< 3°` | 2.009° |
| 11 | **稳态姿态误差（roll/pitch）** | `< 3°` | **0.0005°** |
| 12 | **偏航漂移率** | `< 1.0 rad/s` | 0.606 rad/s |
| 13 | 推力峰值占比 | `< 95%` | 87.3 % |
| 14 | 最大机体角速度 | `< 8 rad/s` | 1.876 rad/s |
| 15 | 负载终位置接近目标 | 欧氏距离 `< 2 cm` | 3.63 mm |
| 16 | **绳索长度不变量 `‖挂点−无人机‖ ≡ l`** | `< 1e-9 m` | **1.67e-16 m**（见 §9.7） |

> **第 11、12 项是按轴拆分的**，这是 3 机版的关键改动。
> 早期用单一的 `steadyAttitudeError < 6°`，必然 FAIL（三轴范数 8.6°），
> 而那 8.6° **全部**来自偏航欠驱动轴的漂移 —— roll/pitch 只有 0.0005°。
> 把三轴混在一起判定会误导人以为"姿态环没整定好"。详见 §9.5。
>
> **第 16 项是绳索特有的断言**：绳在绷紧时与刚性连杆力学完全相同，
> 唯一可数值验证的差别就是"长度必须严格守恒"（这正是**绷紧**的数学含义）
> 与"张力恒为正"（单边约束 `mu >= 0`，已由第 7 项覆盖）。
> 实测偏差 `1.67e-16 m`，是浮点舍入量级。详见 §9.7。

## 7. 与真实平台的接口

```matlab
% 仿真输出的每一步可直接映射到 cflib
ratesDeg = rad2deg(sim.command(k).omegaCommand);
thrustPct = sim.command(k).thrustPercentage;
```

```python
cf.commander.send_setpoint_manual(
    roll=float(rates_deg[0]),
    pitch=float(rates_deg[1]),
    yawrate=float(rates_deg[2]),
    thrust_percentage=float(thrust_pct),
    rate=True,
)
```

对应关系：

| 仿真量 | 真实平台 |
|---|---|
| `thrustPercentage` | `send_setpoint_manual` 的 `thrust_percentage` |
| `omegaCommand` | `send_setpoint_manual` 的 `roll/pitch/yawrate` |
| 推力一阶执行器 | 电机 + 桨叶动态 |
| 速率环（一阶等效） | 固件速率 PID + 机载陀螺仪 |
| 理想状态反馈 | 动捕 / 光流 + 卡尔曼滤波 |

真实部署时需要注意：

1. 仿真使用理想状态反馈，未加入动捕噪声和延迟，实机需要滤波与状态估计；
2. 仿真推力是"总推力百分比"的理想线性映射，实机需用推力台标定
   `f -> thrust_percentage` 曲线；
3. 负载与连杆的建模假设为**刚性、无质量连杆**。原文的关键假设（条件 (13)）
   要求 `rank(P) >= 6`，也就是 **`n >= 3` 架机**。
   本仿真已按 `n = 3` 实现；若改为绳索（单向拉）、需要引入松弛判据
   （张力为负时切换为自由绳模型）；
4. **多机实现的关键点**：分配矩阵 `P = [I … I; hat(rho_1) … hat(rho_n)]`（6×3n）
   的伪逆，以及论文 (17)-(19) 中对每个 `a_i` 的构造。n = 3 该仿真的
   `cond(P P') = 276.87`，条件良好；挂点越接近共线，条件数越差；
5. **同步要求**：本仿真假定三架机的状态在同一控制器里集中计算、同一拍下发。
   实机上若改为分布式，会引入通信延迟，`a_i` 的构造需要改为基于各自可测量的量
   （或加延迟补偿），否则会损失一拍的相位裕度。这些已经在仿真里排除了，
   所以**不要**把中心化的 `desiredTensions` 直接照搬到分布式实现中。

## 8. 已知限制

- 连杆刚性和无质量假设在论文中同样是基础假设，未计入绳/杆的弹性与阻尼；
- 姿态角速度环用一阶模型等效固件速率环，未建模电机饱和与陀螺噪声；
- **负载偏航通道为欠驱动自由轴**（`kR(3) = kOmega(3) = 0`）。这是本仿真
  经过系统辨识得到的结论，不是参数没调好：在该构型下偏航力矩需求会变成
  张力的纯水平分量，被连杆方向投影完全丢弃，构成纯正反馈。
  详见 §5.1 与 §9.1 修复 3。后果是负载偏航角缓慢漂移
  （实测 30 s 约 0.61 rad/s，60 s 约 1.0 rad/s，77.2 s 后发散）。
  若演示时长超过 30 s，位置精度会开始下降、连杆误差持续增长；
  工程解法见 §6.3；
- **连杆方向不存在真稳态**：它随偏航漂移以约 **+0.057 °/s** 单调增长
  （实测 0.79° @5 s → 2.32° @30 s → 6.43° @60 s）。因此自检的
  "稳态连杆误差"实际是 30 s 窗口的均值，判据放宽到 3°（见 §6.2）；
- 摆方向积分器默认关闭（`kqIntegral = 0`）：实测开了之后连杆误差与位置误差
  **都略微变差**（见 §9.1 修复 6c），因为它追的是偏航漂移注入的低频误差；
- 本仿真使用**理想状态反馈**，未注入测量噪声与延迟。实机需补状态估计；
- 初始瞬态的最大位置误差来自负载从起点到目标点的重定位（起点 `[0.10,-0.06,0.45]`
  → 目标 `[0,0,-0.35]`），若需要更平滑的演示，可把 `cfg.initial.position`
  调近目标点，或改用 `cfg.referenceFcn` 提供渐变轨迹；
- **`kq = 55` 已是稳定上限**：`kq = 70` 即发散于 9.2 s。不要按单机版的
  经验"把摆环调快"（见 §9.1 修复 6b）。

## 9. 调试记录与验证工具

### 9.1 修复记录

#### 修复 3（3 机版最关键）：负载偏航通道导致闭环发散 —— 关闭偏航增益

**现象**

n = 3 版本的第一次实现完全发散，用户实测输出：

```text
[FAIL] 稳态位置误差 < 2 cm        | 97.6128 m
[FAIL] 连杆张力始终为正            | min -125.7 N / max 217.2 N
[FAIL] 推力峰值占比 < 95%          | 100.0 %
```

**排查过程（每一步都排除了一个嫌疑）**

1. **先怀疑动力学写错** —— 用论文 (18) 作为判据交叉验证 6×6 降阶形式：
   残差 2.3e-5 N，而另一个候选写法（把 `alpha_i` 当独立未知量，
   扩成 `(6+3n)` 系统）残差 2.4e-2 N，差三个数量级。**动力学排除**。
2. **再怀疑张力分配符号** —— 诊断脚本报告 `sum hat(rho_i) mu = -Md`，
   看起来是符号错。仔细核对后发现是**诊断脚本自己的 bug**
   （写成了 `hat(RHO[:,i]).T` 而不是 `hat(RHO[:,i])`）。
   修正后 `P · diag[R0'] · mu = [R0' Fd; Md]` 精确成立，三个力矩通道
   增益均为 1.0 机器精度。**分配排除**。
3. **怀疑积分器 / 步长** —— `dt` 从 0.002 缩到 0.00025（8 倍），
   发散只是被推迟，仍漂移 0.14 m。**离散化排除**。
4. **怀疑调参** —— 系统性扫描，全部失败（详见 §5.1 的表格）：
   `kR0[3]` 降到 1/50、1/77 名义值、`kO0[3] = 0.002`、
   去掉 `Mz` 方向指令、限幅 `mu_id`、绕道阻尼、提高 `kR0[3]`。
   **调参排除** —— 这迫使我们相信是结构性问题。
5. **决定性试验 `_test_channels.py`** —— 逐通道开关，结果见 §5.1 的表格：
   **只有偏航通道是毒药**（单独保留 roll 或 pitch 都收敛，
   单独保留 yaw 立刻发散）。
6. **机制定量 `_diag_projection_loss.py`** —— 测出力矩交付率
   24.9 % → 0 %，`q_id` 被甩到 `[-0.968, 0, 0.250]`（几乎水平）。
   **机制完全清楚**。

**根因**：见 §5.1 的四步链。一句话概括 ——
**论文 (13) 的 `rank(P) >= 6` 只保证分配方程有解，不保证每个通道可控**。
本构型（近似竖直连杆 + 水平面内挂点，`rho_z = -0.010` 远小于 `rho_xy ≈ 0.085`）
下，偏航力矩需求会解出**纯水平**的张力扰动，该扰动被 (27) 的投影
`mu_i = q_i q_i' mu_id` 全部丢弃，于是偏航环**只有副作用（污染 `q_id`）、
没有正作用（零力矩交付）**，形成纯正反馈。

**修复**（`crazyflie_slung_parameters.m`）：

```matlab
payloadInertia = cfg.payload.inertia;
wnLoad   = 2 * pi * [6.0; 6.0; 0.0];   % ★ 第 3 项为 0：偏航通道关闭
zetaLoad = 0.90;
kRLoad     = diag(payloadInertia) .* wnLoad.^2;
kOmegaLoad = 2 * zetaLoad * wnLoad .* diag(payloadInertia);
% 结果：kR = [0.382778; 0.382778; 0]，kOmega = [0.0182763; 0.0182763; 0]
cfg.loadController.yawChannelEnabled = false;
```

配套在 `crazyflie_slung_controller.m` 的 `Md` 计算处加了 9 行注释，
说明"这里不做分支处理，但千万不要把第 3 个增益改回非零"。

**修复后结果**：稳态位置误差 15.95 mm（`ki=0.60`）/ 4.24 mm（`ki=1.60`），
三根张力 `[0.2620, 0.2620, 0.2619] N`（理论 0.2616 N），推力峰值 87.3 %，
三机始终在负载上方。**从完全发散到全部指标达标。**

#### 修复 6：3 机版的三次增益重标定（`ki` / `kq` / `kqIntegral`）

单机版整定的增益不能直接搬到 3 机，原因是**摆环通过挂点位置与负载姿态环强耦合**。
用 `_sweep_3drone.py` 与 `_scan_ki_3drone.py` 重新扫描
（30 s 仿真，取后 20 % 为稳态窗口）：

**(a) `ki`（负载位置积分）—— 唯一可以放心加大的一维**

| `ki` | 稳态位置误差（24–30 s） | 连杆误差 | 推力峰值 |
|---|---|---|---|
| 0.60 | 15.95 mm | 1.812° | 87.3 % |
| 0.90 | — | 1.811° | 87.3 % |
| 1.20 | — | 1.811° | 87.3 % |
| **1.60** | **4.24 mm** | **2.009°** | **87.3 %** |

位置误差单调下降，连杆误差与推力峰值**基本不变** → `ki` 只作用于负载平动通道。
**采用 `ki = 1.60`。**

**(b) `kq`（连杆方向增益）—— 已到稳定上限，不能再加**

| `kq` | 结果 |
|---|---|
| **55** | **收敛（采用）** |
| 70 | 发散于 9.2 s |
| 85 | 发散于 5.3 s |
| 100 | 发散于 3.5 s |
| 120 | 发散于 3.0 s |

机理：偏航漂移使挂点方位角持续转动，**期望连杆方向本身在低频变化**。
提高 `kq` 会让连杆环更"硬"地追赶这个漂移，反而把漂移能量放大进姿态环。
这与单机版"`kq` 越大越好"的直觉相反 —— 多机情形下摆环与负载姿态环
通过挂点位置强耦合，不能独立整定。

**(c) `kqIntegral`（连杆方向积分）—— 实测证明有害无益**

| `kqIntegral` | 稳态位置误差（20 s 窗口） | 连杆误差 |
|---|---|---|
| **0（采用）** | **23.99 mm** | **1.410°** |
| 0.3 | 24.00 mm | 1.412° |
| 0.6 | 24.01 mm | 1.414° |
| 1.0 | 24.01 mm | 1.417° |
| 1.5 | 24.02 mm | 1.420° |

开了积分**略微变差**。因为连杆方向的残差不是常值扰动（积分器能消），
而是偏航漂移持续注入的低频误差，积分器追不上，只引入相位滞后。

> **三条经验**（写给后来调参的人）：
> 1. 3 机情形下 **`ki` 是安全维**，只管加大；
> 2. **`kq` 是危险维**，55 已是上限，改大立刻发散；
> 3. **`kqIntegral` 不要开** —— 先解决偏航漂移，否则任何"消除残差"的手段
>    都只是在追赶一个追不上的目标。

#### 修复 4：`vehicleRpy` 维度错误导致 `rpyToRotm` 下标越界

**现象**

```text
>> crazyflie_slung_demo
Index exceeds the number of array elements. Index must not exceed 2.
出错 crazyflie_slung_parameters>rpyToRotm (第 234 行)
出错 crazyflie_slung_parameters (第 216 行)
    cfg.initial.vehicleR(:, :, i) = rpyToRotm(cfg.initial.vehicleRpy(:, i));
出错 crazyflie_slung_demo (第 26 行)
```

**原因**

`rpyToRotm` 里第 234 行是

```matlab
roll = rpy(1);  pitch = rpy(2);  yaw = rpy(3);
```

它要求传入的是长度为 3 的向量。但 `vehicleRpy` 的初始化写成了

```matlab
cfg.initial.vehicleRpy = deg2rad([5, -4; -3, 6; 4, 2].');   % 错：转置后是 2x3
```

`[5,-4; -3,6; 4,2]` 是 3×2，转置后是 **2×3**，于是 `vehicleRpy(:, 3)` 只有
**2 个元素**，`rpy(3)` 越界。注意此时 `vehicleRpy(:, 1)` 和 `(:, 2)` 是合法的
（各有 3 个元素），所以只有第 3 架机报错 —— 这类"部分下标合法"的错误
最容易被误读成"循环写错"。

**修复**

```matlab
% 3 x n，每列一架（列 = 机号，行 = roll / pitch / yaw）
cfg.initial.vehicleRpy = deg2rad([[5; -4; 4], [-3; 6; 1], [4; 2; -3]]);
```

同时在赋值处加了一道维度守卫：

```matlab
assert(size(cfg.initial.vehicleRpy, 1) == 3, ...
    'cfg.initial.vehicleRpy 必须是 3 x n（每列一个 roll/pitch/yaw）');
assert(size(cfg.initial.vehicleRpy, 2) == cfg.vehicle.count, ...
    'cfg.initial.vehicleRpy 的列数必须等于 cfg.vehicle.count');
```

这样以后改机数时若忘记同步初始化矩阵，会在参数解析阶段就给出明确报错，
而不是等到深层函数里下标越界。

#### 修复 5：numpy 镜像的 `reshape` 顺序错误（曾导致一次错误的"符号"结论）

Python 镜像里用 `reshape(v, 3, n)` 把扁平向量还原成 `3×n` 的逐机张量。
**numpy 默认是 C 序（行优先），MATLAB 是列优先**，两者结果不同 ——
这会让每架机的张力"串位"，看起来像动力学算错了。

修复：所有从 Python 内部扁平向量还原 `3×n` 的地方统一写
`reshape(v, 3, n, order='F')`。

> 这条只影响验证脚本，不影响交付的 `.m` 文件。记录下来是因为它曾经让我
> 误判"张力分配符号写反"，浪费了一轮排查。**做跨语言镜像时，
> 内存布局是必查项。**

#### 修复 0（最关键）：动力学耦合项符号写反，导致仿真在十几秒后发散

**现象**

`crazyflie_slung_demo` 能跑完不报错，但结果完全发散：自检只有 2/11 通过。

```text
[FAIL] 稳态位置误差 < 2 cm        | 97.6128 m
[FAIL] 连杆张力始终为正            | min -125.6810 N / max 217.1653 N
[FAIL] 推力峰值占比 < 95%          | 100.0 %
      负载终位置                  | [3.8069 -19.2234 16.7863] m（目标 [0,0,-0.45]）
      最大位置误差                | 181.87 m
```

**根因：论文 (5) 式右端两个耦合项都带负号，代码里写成了正号**

论文（Lee 2018，式 (5)，即 2014 版的式 (6)）原文是

```text
Mq(x0_ddot − g e3) − Σ mᵢ qᵢqᵢᵀR₀ρ̂ᵢΩ̇₀
      = Σᵢ u∥ᵢ − mᵢlᵢ‖ωᵢ‖²qᵢ − mᵢqᵢqᵢᵀR₀Ω̂₀²ρᵢ       ← 注意是减号
```

而式 (6)（转动方程）右端括号里是**与式 (5) 完全相同的那个组合**：

```text
( J₀ − Σ mᵢρ̂ᵢR₀ᵀqᵢqᵢᵀR₀ρ̂ᵢ ) Ω̇₀ + Σ mᵢρ̂ᵢR₀ᵀqᵢqᵢᵀ(ẍ₀−ge3) + Ω̂₀J₀Ω₀
      = ΔR₀ + Σᵢ ρ̂ᵢR₀ᵀ ( u∥ᵢ − mᵢlᵢ‖ωᵢ‖²qᵢ − mᵢqᵢqᵢᵀR₀Ω̂₀²ρᵢ )
```

原实现有两个错误：

1. `rhsX = uParallel + parallelCoupling + Mq*g*e3;` —— 耦合项符号反了，应为
   `uParallel - parallelCoupling`；
2. `rhsW` 里又额外加了一次 `R0Hat.' * R0.' * uParallel` —— 等于把 `u∥` 算了两遍。
   正确写法是 `rhsW = R0Hat'*R0'*rhsX - cross(Omega0, J0*Omega0)`，
   其中 `rhsX` 已经是那个括号里的组合。

**为什么危害这么大**：符号一反过来，平动–转动耦合就从"负反馈"变成"正反馈"。
系统在头十几秒看起来还正常（负载缓慢偏离），随后进入指数增长，张力冲到 217 N、
推力饱和、负载掉到目标下方 17 m。

**修复**（`crazyflie_slung_dynamics.m`）：按论文原式重写为求解 `X = ẍ₀ − g eₑ` 的形式

```matlab
Mblock = [Mq,                     -m * Q * R0 * R0Hat; ...
          m * R0Hat.' * R0.' * Q, J0 - m * R0Hat.' * R0.' * Q * R0 * R0Hat];
rhsX = uParallel - parallelCoupling;                          % 论文 (5)
rhsW = R0Hat.' * R0.' * rhsX - cross(Omega0, J0 * Omega0);    % 论文 (6)
solution = Mblock \ [rhsX; rhsW];
x0DotDot = solution(1:3) + g * e3;      % 还原 x0_ddot = X + g e3
```

**验证**：修复后 60 s 长时仿真，全部指标单调收敛（见 §6.1、§6.2）。

#### 修复 0b：另外两处 1/dt 数值放大（会让姿态误差缓慢增长）

修好符号后系统收敛了，但发现**姿态误差随时间缓慢增长**（0.6° → 8.9°），
机体角速度长期维持在 4.2 rad/s。原因是控制器里有若干"靠相邻步长差分求导"的
前馈项，被 `1/dt` 放大：

| 项 | 放大量 | 处置 |
|---|---|---|
| `qdDotDot = (qdDot − 上一步)/dt`（论文 (33) 的 `ω̇_d` 前馈） | `1/dt² = 2.5e5` | **删除**。静态目标下理想值恒为 0，留着纯属噪声源 |
| `Ω_c = so3Log(Rc_prev' Rc)/dt`（期望姿态角速度前馈） | `1/dt = 500` | **置零**。同理，稳态期望姿态恒定，理想值就是 0 |
| `qdDot = (qd − 上一步)/dt`（`ω_d` 前馈） | `1/dt = 500` | **保留**。实测这一项是承重的：去掉后摆环失去对期望方向的跟踪能力，系统反而发散（位置误差涨到 2.7 m） |

实测对照（60 s，其余参数相同）：

```text
Ω_c 前馈开启：姿态误差 0.6 → 8.9 deg 持续增长，机体角速度维持 4.2 rad/s
Ω_c 前馈关闭：姿态误差 0.6 → 2.3 deg 单调衰减，机体角速度 0.15 rad/s
```

> 结论：**能用解析求导就不要用差分**；非要用差分，必须先低通滤波或限幅，
> 绝不能把裸差分结果乘 `1/dt` 直接塞进反馈通道。

#### 修复 0c：作用力模型改为物理正确的 `-f R e3`

原实现把仿真中的作用力构造成"保持理想方向、只缩放幅值"：

```matlab
actualForce = command.totalForce * (actualThrust / command.thrustCommand);   % 错误
```

这会带来两个问题：

1. **姿态环被架空**。真实四旋翼的合力方向由**实际机体轴**决定，`-f R e3`；上式
   强行把力锁在理想方向 `u` 上，姿态再偏也不影响力方向，论文最核心的
   "姿态环 + 位置环"级联结构被抹掉了。这也解释了为什么早期版本扫描姿态增益
   "完全没有影响"——因为那条通路确实被切断了。
2. **隐藏的正反馈**。当机体大幅倾斜时 `f_cmd = -u'Re3 → 0`，该式会把力幅值放大到
   无穷。

修复后：

```matlab
actualForce = -actualThrust * (state.rotation * [0; 0; 1]);
```

只有姿态收敛到 `Rc` 时才有 `-f R e3 → u`，这正是论文奇异摄动结论的前提。

#### 修复 1：`crazyflie_slung_simulation.m` 调用未定义的 `normalizeVector`

**现象**

```text
函数或变量 'normalizeVector' 无法识别。
出错 crazyflie_slung_simulation (第 55 行)
state.linkUnit = normalizeVector(cfg.initial.linkUnit);
```

**原因**

MATLAB 的局部函数是**文件私有**的。该文件末尾把归一化工具函数命名为
`projectUnit`，但第 55 行却按其它文件的习惯写成了 `normalizeVector`，
于是同一份代码里"调用的名字"和"定义的名字"不一致。

**为什么之前的数值验证没发现**

数值验证用的是 Python 逐行镜像（`_verify_python/verify_slung.py`），
Python 没有"文件私有函数"这条规则，所以镜像可以正常运行，
而真实 MATLAB 会在此处直接报错。这是一类**只有 MATLAB 语义才会暴露**的
缺陷，必须靠静态检查而不是数值实验来发现。

**修复**

统一命名：把该文件中的 `projectUnit` 重命名为 `normalizeVector`，
与 `parameters.m` / `dynamics.m` / `controller.m` 保持一致。

```matlab
% 第 55 行（初始连杆方向）
state.linkUnit = normalizeVector(cfg.initial.linkUnit);
% 第 144 行（积分后重新投影到单位球面）
state.linkUnit = normalizeVector(state.linkUnit + dt * state.linkRate);
% 文件末尾
function q = normalizeVector(q)
```

该修复是**纯重命名**，不改变任何数值行为（`projectUnit` 与
`normalizeVector` 的实现逐字相同，退化分支都返回 `+e3`）。

#### 修复 1b：`nargout` 分派漏掉匿名句柄的 `-1` —— 又一类"只在 MATLAB 语义层暴露"的缺陷

**现象**

八字工况第一次运行时，配置打印全部正常，但一进入仿真就报错：

```text
此类型的变量不支持使用点进行索引。
出错 crazyflie_slung_simulation>callReferenceFunction (第 290 行)
position = data.position;
```

**原因（关键）**

`cfg.referenceFcn = @(t) crazyflie_slung_reference(t, cfg)` 是一个**匿名函数句柄**。
MATLAB 对匿名函数执行 `nargout()` **恒返回 `-1`**（输出个数"未知"，
因为匿名函数的输出是由其 body 的函数调用转发而来的）。

而旧的分派逻辑写的是：

```matlab
numberOfOutputs = nargout(referenceFcn);
if numberOfOutputs >= 6
    ...
elseif numberOfOutputs == 5
    ...
elseif numberOfOutputs == 4
    ...
else
    data = referenceFcn(t);     % ← -1 掉进这里
    position = data.position;   % ← 对数值向量取字段 → 报错
end
```

`-1` 既不 `>= 6`，也不 `== 5` / `== 4`，于是落进最后那个"按 struct 解释"的
`else` 分支，把返回的 3×1 数值向量当成结构体取 `.position` → 直接报错。

**为什么潜伏了这么久**

`crazyflie_slung_reference.m` 本身是**返回 6 个输出**的普通函数，
输出个数完全明确。问题只出在"**经匿名句柄转发**"这一步丢了 `nargout` 信息。

而在此之前，唯一被真正执行过的轨迹分支是**静态悬停**（`cfg.referenceFcn = []`），
它走的是 `isempty` 那条路，**根本不经过 `callReferenceFunction`**。
八字工况是第一次让非空句柄上场，于是这条从未被执行的路径立刻就暴露了。

**为什么数值验证没发现**

Python 镜像里 `reference` 就是一个普通 Python 函数，直接调用、直接解包，
**没有 `nargout` 这套语义**。镜像的一切都正常，MATLAB 一跑就崩。
这和"修复 1"的 `normalizeVector` 属于同一类：**文件私有函数、`nargin`/`nargout`、
匿名句柄转发，这些语义层缺陷数值实验永远覆盖不到**。

**修复（三点）**

1. **显式处理 `-1`**，并为它单开一条路径，与已知个数精确分派彻底分开：

```matlab
numberOfOutputs = nargout(referenceFcn);

if numberOfOutputs < 0
    % 匿名句柄：nargout 恒为 -1，只能逐级回退试探
    [...] = callReferenceAnonymous(referenceFcn, t);
    return
end

% nargout 已知：按确切个数精确分派（>= 6 / == 5 / == 4 / == 3）
...
% 其余（0~2 个输出）：单输出兜底
```

2. **匿名路径逐级回退**：依次尝试 6 → 5 → 4 → 3 个输出，
   全部失败才按单输出解释（struct 或位置向量）。
   本项目真实契约会**第一发就命中 6 个输出**，不回退也不告警。

3. **退化必须告警，不能静默**：任何"用默认值顶替"的分支（5/4/3 输出、
   struct 缺字段、退化为位置向量）都会调一次 `warnDegraded`。
   用 `persistent` 保证**只警告一次**，否则 12500 个积分步会刷满命令窗口。
   另外，若逐级回退全部失败，会 `rethrow` **第一个**错误，
   以免把参考函数内部的真实 bug（维度不符等）伪装成"只返回位置"而静默降级。

**新增防线**

- `_lint_matlab.py` 新增**第 6 类检查** `nargout_dispatch_risk()`：
  对每个 `var = nargout(<标识符>)` / `nargin(<标识符>)`，
  要求同文件内存在对 `var` 的**负数守卫**（`var < 0` / `var <= -1` / `var == -1`）。
  正则要求括号内是裸标识符，所以 `nargout(@namedFn)` 这类**字面句柄**不受影响
  （命名函数的 `nargout` 是已知的）。
- `_test_lint_regress.py` 扩充：用**当初那段真实的错误分派**做输入，
  断言它被报出、修好的版本不报出、且**真实交付文件**是干净的。
- 新增 `_test_reference_dispatch.py`：结构审计（从真实 .m 里确认 `-1` 守卫在前、
  匿名路径第一发请求 6 个输出、struct 兜底存在）+ 逻辑仿真
  （7 种契约 × `nargout = -1/6/7` 断言输出完整、默认值补齐符合预期）。

> ★ **这次修复同时暴露了 `_lint_matlab.py` 自己的两个盲区**（都已修）：
> 1. `rethrow` 是 MATLAB 内置函数，但不在白名单里 → 误报"未定义"；
> 2. `catch err` 的 `err` 与 `persistent firedKinds` 的 `firedKinds`
>    都属于**定义**，但检查没登记它们。
>    更隐蔽的是：登记原本用 `setdefault`，而登记是**分多个循环**做的，
>    于是"先跑到的循环"胜出而不是"行号最小的胜出" ——
>    正文赋值 `firedKinds = {}`（388 行）抢在了声明 `persistent firedKinds`（386 行）前面，
>    把 387 行的首次使用误报成"先用后定义"。
>    已改为 `register()` 取 **min**，即真正的"首次定义行"。
>    **教训：多个循环共同填充一个索引时，"谁先跑到"与"谁最早"是两回事。**

#### 修复 2：`yyaxis` 双纵轴图的 legend 警告

`crazyflie_slung_visualization.m` 的第 5 个子图中，legend 一次性给了 4 个
标签，但其中"张力"属于**左轴**对象、其余 3 个属于**右轴**对象。`yyaxis`
下左右两侧分属不同的 ruler，跨轴混用句柄会触发
`警告: Ignoring extra legend entries.`。

修复方式：legend 只标注右轴对象，左轴用 `ylabel('连杆张力 (N)')` 说明，
并在标题中写明"（左轴）（右轴）"。现在运行仿真不会产生任何警告。

同理，子图 4 与子图 5 中的 `yline` 也一并改成了普通 `plot` 画的水平线：
`yline` 返回的 `ConstantLine` 对象在不同 MATLAB 版本中的"可入图例性"不一致，
也可能触发同类警告。改用 `plot` 后行为完全确定：

```matlab
% 原来：plot([time(1), time(end)], repmat(y, 1, 2), '--', ...)
% 现在：hLimit = plot([time(1), time(end)], repmat(cfg.vehicle.maxTotalThrust,1,2), ...)
```

### 9.2 静态检查脚本 `_verify_python/_lint_matlab.py`

因为约束是"不得调用本机 MATLAB"，数值镜像无法覆盖 MATLAB 的**语义**层
（函数可见性、字段名拼写、`nargin`/`nargout` 语义）。为此写了一个静态检查脚本，
目前覆盖**六类**问题：

| 检查项 | 内容 |
|---|---|
| 1. 函数可见性 | 每个文件里被调用的函数，是否在该文件内定义 / 是本文件夹内的另一个 `.m` / 是 MATLAB 内置函数 |
| 2. `cfg.*` 字段路径 | 所有 `cfg.<组>.<字段>` 访问是否都能在 `crazyflie_slung_parameters.m` 中找到定义 |
| 3. 结构体字段一致性 | `state.*` / `command.*` / `memory.*` / `derivative.*` / `summary.*` / `desired.*` 的读写字段是否一致 |
| 4. 代码块 / 括号配平 | 每条 `function/if/for/while/switch/try` 是否有配对的 `end`（正确排除 `x(end)` 这种下标用法），括号是否闭合 |
| 5. 变量使用顺序 | 同作用域内"先读后定义"的变量（MATLAB 只在运行期报"函数或变量无法识别"，镜像永远抓不到） |
| 6. `nargout`/`nargin` 分派 | ★ 匿名函数句柄的 `nargout` 恒为 `-1`：若分派没有负数守卫，`-1` 会掉进 `else` 分支被当成 struct，报"此类型的变量不支持使用点进行索引"（见 §9.1 修复 1b） |

运行方式：

```bash
python _verify_python/_lint_matlab.py
```

当前结果（全部通过）：

```text
汇总: 可见性 0 | 使用顺序 0 | 分派 0 | cfg路径 0 | 字段 0 | 配平 0
```

配平检查给出了每个文件的块计数，可用于核对新增代码时没有漏写 `end`：

```text
crazyflie_slung_parameters.m   : 块起始 17 / end 17 | 括号净深度 0
crazyflie_slung_dynamics.m     : 块起始  9 / end  9 | 括号净深度 0
crazyflie_slung_controller.m   : 块起始 23 / end 23 | 括号净深度 0
crazyflie_slung_simulation.m   : 块起始 54 / end 54 | 括号净深度 0
crazyflie_slung_visualization.m: 块起始 49 / end 49 | 括号净深度 0
crazyflie_slung_demo.m         : 块起始 34 / end 34 | 括号净深度 0
```

> **第 5、6 两类各自的回归测试**：`_test_lint_regress.py`
> （用当初真实的错误代码做输入，断言"能抓到 bug"且"不误报修好的版本"，
> 并核对**真实交付文件**是干净的）。

其中第 2 项覆盖了全部 12 个参数组：

```text
attitudeController(5)  initial(11)  link(1)   linkController(5)
loadController(9)      payload(4)   rateLoop(1)  reference(2)
simulation(4)          target(5)    vehicle(8)   visualization(7)
标量字段: referenceFcn
```

### 9.3 数值验证工具 `_verify_python/`

| 文件 | 用途 |
|---|---|
| `_multi_mirror.py` | **当前有效的镜像（3 机版）**：逐行对应修正后的动力学 / 控制器 / 积分器，输出 §6.1 的全部指标 |
| `_lint_matlab.py` | 静态检查（见 §9.2，六类） |
| **`_test_lint_regress.py`** | ★ **静态检查的回归测试**：用当初真实的错误代码（`armLength` 先用后定义、`nargout` 分派漏 `-1`、`persistent`/`catch` 绑定）做输入，断言"能抓到 bug""不误报修好的版本""真实交付文件干净" |
| **`_test_reference_dispatch.py`** | ★ **`callReferenceFunction` 分派的结构审计 + 逻辑仿真**：从真实 `.m` 确认 `-1` 守卫在前、匿名路径第一发就请求 6 个输出；再用假参考函数覆盖 7 种契约 × `nargout = -1/6/7`，断言输出完整、默认值补齐与告警符合预期 |
| `_verify_final_fix.py` | 用最终参数集跑 30 s 验证偏航修复有效 |
| **`_test_channels.py`** | **决定性试验**：逐通道开关负载姿态环，证明"只有偏航通道发散" |
| **`_diag_projection_loss.py`** | **机制定量**：测出力矩交付率 24.9 % → 0 %，`q_id` 甩向水平 |
| `_check_alloc_sign.py` | 纠正误判：证明张力分配符号是**正确**的（脚本自身 bug） |
| `_pinpoint_sign.py` | 定位上述脚本 bug（`hat(RHO[:,i]).T` 写错） |
| `_check_allocation.py` | 测 `cond(P P') = 276.87`、三通道增益均为 1.0 |
| `_diag_qid_sensitivity.py` | 测 `Mx`/`My` 对 `q_id` 的敏感度为 0，`Mz` 为 14.99 rad/(N·m) |
| `_gain_chain.py` | 量化环路：`M_eff_z = +0.617 N·m`，与输入同号 → 正反馈 |
| `_trace_path.py` / `_trace_internal.py` | 追踪发散路径与控制器内部量 |
| `_check_longterm_3drone.py` | 长时行为：偏航是自由积分轴，77.2 s 发散；连杆误差斜率 +0.057 °/s |
| `_scan_2d_gains.py` / `_scan_bandwidth.py` / `_scan_kO0z.py` / `_test_kR0z.py` | 增益扫描，证明"任何非零偏航增益都发散" |
| `_fix_yaw_loop.py` / `_test_qid_patch.py` / `_fix_mz_dir.py` / `_fix_mu_limiter.py` / `_fix_yaw_bypass.py` | 五种尝试性修复，全部失败（反证结构性） |
| `_diag_inherent_damping.py` | 负载姿态固有阻尼测量 |
| `_diag_yaw_torque.py` | 偏航力矩交付检查（曾被自身 bug 误导） |
| `_sweep_3drone.py` | 3 机版增益扫描（摆环 `kq` / 摆环积分 `kqIntegral` / 位置积分 `ki`） |
| `_plot_results_3drone.py` | 生成 `expected_results.png`（3 机版，含按轴姿态误差分解图） |
| `_finalcheck.py` / `_diag60.py` / `_sweep5..7.py` | 单机版的断言 / 长时诊断 / 增益扫描，保留作历史对照 |
| `_plot_results.py` | 单机版绘图，已被 `_plot_results_3drone.py` 取代 |

> ⚠️ 这些 Python 文件**不属于交付代码**，只是"不能运行 MATLAB"前提下的验证手段。
> 交付物是 6 个 `.m` 文件，把它们放在同一个文件夹里即可独立运行。
>
> **阅读顺序建议**（如果要复核偏航结论）：`_test_channels.py`（现象）
> → `_diag_projection_loss.py`（机制）→ `_check_allocation.py`（排除分配）
> → `_multi_mirror.py`（最终结果）。其余脚本是排查过程中的旁证。

### 9.4 三维可视化的改进（针对"看不出仿的是什么"）

早期版本的动画有两个问题，都会被误认为"没有无人机模型、没有负载模型"：

1. **坐标轴被自动缩放到整条轨迹**。发散时轨迹跑到 200 m，坐标轴跟着拉到
   上百米量级，而机体只有 0.093 m、负载 0.10 m，于是两者都缩成了一个点。
   现在改为**用"起点 + 目标 + 最小跨度（`axisSpanMin = 0.9 m`）"框定视野**，
   不再随轨迹自动缩放。
2. **几何太简略**。原来只有一条穿心直线代表机臂、几个小点代表电机、负载只有
   一个薄片加顶面填充。现在改为完整的可视模型：
   - 四旋翼：机体中心板（长方体 patch）+ 4 条机臂 + 4 个旋翼圆盘 +
     随 `rotorSpinHz` 旋转的桨叶；对角桨叶同色（前红后蓝）以便分辨机头朝向；
   - 负载：完整 8 顶点 / 6 面的长方体刚体 patch，并标出**每个**挂点 `rho_i`
     （3 机时可见互成 120° 的三个挂点）；
   - 连杆：从各机体中心分别连到对应的负载挂点（3 根独立连杆）。

   `vehicleScale` / `payloadSizeScale` 只是"看得清"的显示缩放
   （当前 `cfg.visualization` 中为 `vehicleScale = 1.6`、
   `payloadSizeScale = 1.0`），**不参与任何物理计算**，文件里已注明。

### 9.5 三维可视化里"负载看起来是个圆环"——不是 bug，但已修掉误导性

用户反馈：三维图里负载表现为一个**圆环**。经量化，这是**真实物理现象**：

| 量 | 数值 |
|---|---|
| 负载水平半对角 | 0.141 m |
| 偏航漂移使四角扫出的圆**直径** | **0.283 m** |
| 负载自身**厚度** | 0.020 m |
| 两者之比 | **14.1 倍** |
| 30 s 内负载自转 | **64.5°**（偏航角从 4.0° → 64.5°） |

即：一块 0.02 m 厚的薄板绕偏航轴自转 64°，四角扫出直径 0.28 m 的圆。
侧视图上那圈就是**扫掠轨迹**，不是负载变成了环。这与 §5.1/§6.2 的
"偏航是欠驱动自由轴"是同一件事的两种呈现。

但原可视化有**两处真实的显示缺陷**，会放大误读：

| 缺陷 | 原值 | 后果 |
|---|---|---|
| 负载体轴不可见 | 无 | 看不出负载在自转，只能看到"一坨环" |
| 二维子图 (1) 轴框按整条轨迹自动适应 | 0.808 m | 负载只占画面 **24.8 %**，且初始段主导 |
| 三维动画 `axisSpanMin` 过大 | 2.00 m | 负载边长只占 **10 %**、厚度占 **1 %**，压成薄板 |

**修复**（`crazyflie_slung_visualization.m` + 参数文件）：

1. **给负载画上三色体轴**（红 x / 绿 y / 蓝 z），自转方向与转速一眼可见；
2. 二维子图 (1) 的轴框改为**以稳态窗口内四旋翼集群的质心为中心**，
   跨度取 `max(trajectoryMinSpan, 2×集群横向尺度, 2.2×负载对角线)`。
   实测跨度 0.808 → **0.624 m**，负载占比 24.8 % → **32.1 %**；
3. `axisSpanMin` 2.00 → **0.55 m**，并加下限 `max(3.2×负载对角线, 1.4×连杆长度)`，
   实测跨度降到 0.907 m，负载占比 10 % → **22.0 %**，连杆占比 17.5 % → **38.6 %**；
4. 新增**可视化告警**：负载边长占跨度 < 20 % 时 `warning`，并打印实际比例；
5. **子图 (6) 换成"负载偏航角"**（原来画"机 1 角速度指令 vs 实测"，
   与自检输出重复）。现在能直接看到偏航单调漂移，附带 0.057 °/s 参考斜率线；
6. 动画标题新增**负载偏航角**，不再让观众自己猜那圈是什么。

> **教训**：显示尺度的量级差本身就是一种"错误信息"。0.02 m 的负载放进 2.00 m
> 的坐标框，读者得到的信息是"这不像个负载"，而不是"这是侧视投影的极限情形"。
> 凡是"真实尺寸 / 显示跨度"小于 20 % 的可视化，都应该主动告警。

### 9.6 自检脚本的按轴姿态判据（重要，不要改回单标量判据）早期版本用单一的 `steadyAttitudeError < 6 deg` 判定负载姿态收敛。
加入偏航欠驱动结论后，这个判据必然 FAIL（实测三轴范数 8.6°，全部由偏航贡献）。
现在改为**按轴分开判定**：

```matlab
% roll / pitch 是可控轴（kR(1:2) = kOmega(1:2) 非零），用严格门限
rollPitchAttErr = rad2deg(norm(s.steadyAttitudeErrorVec(1:2)));
checks = addCheck(checks, '稳态姿态误差(roll/pitch) < 3 deg', ...
    rollPitchAttErr < 3, ...);

% 偏航是欠驱动自由轴（kR(3) = kOmega(3) = 0），只检查漂移率有界
yawDriftRate = abs(s.loadBodyRateFinal(3));
checks = addCheck(checks, '偏航漂移率 < 1.0 rad/s', yawDriftRate < 1.0, ...);
```

主程序为此新增了两个日志与两个摘要字段：

| 名称 | 尺寸 | 含义 |
|---|---|---|
| `sim.loadAttitudeErrorLog` | `3 x nSteps` | 负载姿态误差的按轴时间序列 |
| `sim.loadBodyRateLog` | `3 x nSteps` | 负载角速度的按轴时间序列 |
| `summary.steadyAttitudeErrorVec` | `3 x 1` | 稳态窗口的逐轴姿态误差均值 |
| `summary.loadBodyRateFinal` | `3 x 1` | 终态负载角速度 |

实测分解结果（镜像 30 s，稳态窗口取后 20 %）：

```text
稳态负载姿态误差 : [roll pitch yaw] = [ 4.00e-04, -2.00e-04, 14.1416] deg
  -> roll/pitch 范数 = 0.000 deg    （可控轴：完全收敛）
  -> yaw            = 14.142 deg    （欠驱动轴：自由漂移）
负载角速度终值   : [-0.0023, -0.0027, -0.6060] rad/s
  -> 偏航漂移率 = 0.606 rad/s       （< 1.0 rad/s 判据）
```

**这张表是"偏航欠驱动结论"最直接的证据**：roll/pitch 的残差是 1e-4 度
（数值零），偏航是 14.1 度且持续增长。三轴范数 8.6° 完全由偏航贡献。
之前把三轴混在一起判定，会让人误以为"姿态环整定不好"。


---

### 9.7 ★ "悬吊物是**绳**不是刚性连杆"——本仿真的建模依据与验证

#### 9.7.1 问题：到底该按绳还是按刚性连杆建模？

论文标题是 *"Coordinated Control of Multiple Quadrotors Transporting a
**Cable-Suspended** Rigid Body"*，正文 §II 写的是 *"via **massless links**"*，
而 Remark 1 又强调 *"the tension at each cable positive when q_i = q_id"*。
三种说法混在一起，容易让人以为"绳"和"刚性连杆"是两套模型。

**结论：在绷紧状态下，两者在力学上完全等价，本仿真无需改动动力学。**

| | 刚性无质量连杆 | 绳（缆） |
|---|---|---|
| 传力方式 | 只沿 `q_i` 轴向 | 只沿 `q_i` 轴向 |
| 能否受**压** | 能（可推可拉） | **不能**（只能拉） |
| 长度约束 | `‖q_i‖ ≡ 1`（双向刚性） | 绷紧时 `‖q_i‖ ≡ 1` |
| 张力符号 | 任意（±） | **`mu_i >= 0`**（单边） |
| 失效模式 | 无 | `mu_i -> 0` 时松弛，负载自由落体 |

因为两者的**力方向**和**长度约束**在绷紧段完全一致，所以：

- 论文的动力学 (5)-(8)（本项目 `crazyflie_slung_dynamics.m`）**不需要改**；
- 张力分配 (13)(22)(23)（`crazyflie_slung_controller.m`）**不需要改**；
- 绳向环 (27) 与推力/姿态环 (36)-(40) **不需要改**。

唯一多出来的是**单边约束 `mu_i >= 0`**。论文用 Remark 1 的负号
（`q_id = -mu_id / ‖mu_id‖`）来保证"四旋翼始终在负载上方"，
正是为了让这个约束**自动满足**。

#### 9.7.2 验证一：本仿真的张力全程为正（单边约束未被激活）

`_verify_python/_check_slack.py` 实测（30 s，500 Hz）：

```text
全程最小张力     : 0.180903 N   出现在 t = 0.00 s
是否出现过 mu<=0 : 否
绳 1 最小值      : 0.243552 N   最大值 0.497911 N
绳 2 最小值      : 0.180903 N   最大值 0.520568 N
绳 3 最小值      : 0.234644 N   最大值 0.431762 N

悬停每绳理论张力 m0 g / n = 0.2616 N
最小张力相对悬停张力的裕度 : 69.2 %
```

最小张力出现在 **t = 0**（初始瞬态，负载从 −0.45 m 爬升到 −0.35 m 的那一刻），
之后所有时刻张力都在 0.23 N 以上。**裕度 69.2 %**，说明绳在 30 s 里
一直是绷紧的，从未进入松弛段。

因此**当前仿真结果对一个绷紧的绳是物理正确的**：它描述的是
"绳的对立面（松弛段自由落体）从未发生"的那种工况。

#### 9.7.3 验证二：绳长不变量严格守恒

绳的"绷紧"在数学上就是长度不变量：

```text
‖x_veh,i − (x0 + R0 rho_i)‖ ≡ l
```

本项目为此新增了两项：

1. **主程序新增断言字段**（`crazyflie_slung_simulation.m` 的 `computeSummary`）：

```matlab
summary.maxRopeLengthDrift      = maxRopeLengthDrift;
summary.ropeLengthInvariantHolds = maxRopeLengthDrift < 1e-9;
```

2. **自检新增第 16 项**（`crazyflie_slung_demo.m`）：

```matlab
checks = addCheck(checks, '绳索长度不变量 ‖挂点-无人机‖ ≡ l', ...
    s.ropeLengthInvariantHolds, ...
    sprintf('最大偏差 %.2e m (l = %.3f m)', s.maxRopeLengthDrift, ...
    cfg.link.length), '< 1e-9 m');
```

实测（`_verify_python/_verify_rope_invariant.py`）：

```text
最大绳长偏差 : 1.665e-16 m
MATLAB 断言 最大偏差 < 1e-9 m  ->  PASS

★ 其余关键指标（确认新增投影未改变结果）
   稳态位置误差 : 4.25 mm
   稳态绳向误差 : 2.014 deg
   最小张力     : 0.180903 N   (裕度 69.2%)
   推力峰值     : 87.3 %
   张力全部为正 : 是
```

`1.7e-16 m` 就是浮点舍入量级，说明该不变量是**构造性成立**的：
本实现里无人机位置本身就用 `veh = x0 + R0 rho_i - l q_i` 定义
（`crazyflie_slung_simulation.m` 第 211-213 行），所以它不可能漂移。

> **排查方法论教训（重要）**：验证这个不变量时，我一开始用
> `np.einsum('ijn,ik->jkn', R0, RHO)` 算挂点位置，得出了
> **"绳长漂移 4.5 cm = 12.9 %"** 的结论，并据此几乎去改动力学。
> 后来用逐点显式循环复核才发现：**einsum 的下标写错了**
> （`R0` 是 `(3,3,nSteps)`、`RHO` 是 `(3,N)`，`'ijn,ik->jkn'` 把 N 和空间维搞混），
> 手算值与记录值实际吻合到 `1.4e-17`。
> **教训：验证"不变量"这类关键恒等式时，宁可写显式循环，不要为了简洁用 einsum；
> 一旦出现"物理结论被一个小程序 bug 决定"的情况，先用等价但独立的算法复核一遍。**

#### 9.7.4 顺带修掉的一处结构性瑕疵：`q_dot` 的径向分量

`‖q_i‖ ≡ 1` 求导给出 `q_i' q_dot_i ≡ 0`（这也是论文 (1) `q_dot = omega x q`
的结论）。旧代码在积分后只对 `q_i` 做归一化，**没有投影 `q_dot` 的径向分量**，
于是每步残留 `O(dt·|q_dot|^2)` 的径向噪声。实测 `q'.q_dot` 残差达 `-6.371e-4`。

已在 `crazyflie_slung_simulation.m` 与镜像 `_multi_mirror.py` 中同步修正：

```matlab
for i = 1:n
    qi = state.linkUnits(:, i);
    state.linkRates(:, i) = state.linkRates(:, i) ...
        - qi * dot(qi, state.linkRates(:, i));
end
```

这一项只影响数值洁净度，**不改变任何结论**（修正前后稳态位置误差 4.25 mm、
绳向误差 2.014°、推力峰值 87.3 % 完全一致）。

#### 9.7.5 若要覆盖"松弛—再绷紧"的完整过程，需要改什么

当前实现描述的是**始终绷紧**的绳。若想仿真绳松弛后负载自由落体、
再被拽紧的完整过程，需要新增以下三处（当前**未实现**）：

| 步骤 | 要做什么 | 关键点 |
|---|---|---|
| ① 松弛判据 | 每步检查分配出的张力 `mu_id_i` | 若 `mu_id_i <= 0`，说明该绳无法提供所需拉力 → 松弛 |
| ② 松弛段动力学 | 该绳改为**不受力**，负载做**抛体运动** | 负载加速度由其余绳的张力 + 重力决定；`q_i` 变为自由长度方向 |
| ③ 再绷紧判据 | 检查 `‖x_veh,i − 挂点‖` 是否回到 `l` | 触及时需按**非弹性碰撞**处理法向速度（脉冲）或用弹簧-阻尼软化 |

失效的典型工况：负载质量 `m0` 相对三机推力过大（当前 `m0 = 0.080 kg`，
悬停单机推力 0.5804 N = 43.5 % 上限，裕度充足）；或初始摆角过大导致瞬时张力归零。

> **为什么本项目不做松弛段**：论文的控制器与稳定性证明**只对绷紧段成立**
> （Remark 1 正是为了避开松弛）。加了松弛段之后，控制器需要在
> 三种模式（绷紧/松弛/再绷紧）之间切换，属于另一套混杂系统控制问题，
> 超出"复现论文 + 改造成推力/角速度控制"的范围。当前做法
> （把单边约束**验证**出来而不去**激活**它）是忠实的，且已在
> §9.7.2 用 69.2 % 裕度证明其合理性。

#### 9.7.6 术语统一

原先代码与文档里混用"连杆"指代实际是绳的悬吊元件。现已统一：

| 位置 | 改动 |
|---|---|
| 6 个 `.m` 文件全部中文注释 | `连杆` → `绳索`（90 处），仅保留 10 处**有意对比**的"刚性连杆" |
| `crazyflie_slung_parameters.m` 文件头 | 明确写出"物理上是绳（cable），不是刚性连杆"，并注明论文用词与 Remark 1 |
| `crazyflie_slung_demo.m` 自检项名 | `各连杆方向`→`各绳方向`；`所有连杆张力恒为正`→`所有绳索张力恒为正（单边约束 mu >= 0）`；`稳态连杆误差`→`稳态绳向误差` |
| MATLAB 变量名 / 字段名 | **保持 `link` 不变**（`cfg.link`、`linkUnits`、`linkRates`…）。理由：这是论文的 `link` 术语，且改名会牵动 6 个文件、多个镜像脚本与全部日志字段，收益不足以抵消风险。**物理口径已在注释里写明。** |

---

## 10. 绕八字避障工况（论文 Fig. 3 算例 / Fig. 2 快照）—— 用户要求的"起飞 + 环绕两个八字 + 降落"

本节记录**本工况的全部设计决策**，尤其是那条花了十几个诊断脚本才定位到的
调参结论。§10.1~§10.4 是设计，§10.5 是**两个根因**（最关键），§10.6 是调参记录。

### 10.1 与论文数值算例的对应关系

论文 Lee 2018 §V 的 Fig. 3 原文：

> "three quadrotors (n = 3) transport a rectangular box along a figure-eight
>  curve **around two obstacles represented by cones**"

论文给的期望轨迹是 `x0d(t) = [1.2 sin(0.2πt), 4.2 cos(0.1πt), −0.5]ᵀ`，
期望姿态取"第一轴沿路径切向、第三轴平行重力"。

本项目做了**五处工程化改造**，全部集中在 `crazyflie_slung_reference.m`
与 `cfg.figureEight`：

| # | 改造 | 原因 |
|---|---|---|
| 1 | **尺度缩放到 ±1.5 m**，但**保持论文的宽高比** | 用户选定"大尺度，约 ±1.5 m"；纵横比例反过来按论文 Fig. 2 像素实测反推，见 §10.1.1 |
| 2 | **三阶段拼接**（起飞 / 环绕 / 降落） | 用户要求"他存在起飞过程和环绕过程，你添加一个环绕两个八字之后的降落过程" |
| 3 | **期望偏航锁定为 0** | 负载偏航通道结构性不可控，时变期望偏航会激励该通道并发散（详见 §5.1） |
| 4 | **环绕段起止点平移到原点** | 论文的八字是无限绕行的；要做"绕完两个八字后降落"，必须让 τ=0 与 τ=tEnd 落在同一水平位置 |
| 5 | **两端对称包络 `blendTime`** | 让环绕段的进出口速度为零，才能与静止端点的起飞/降落段 C² 拼接；★ 这个参数是本工况稳定性的**决定因素**，见 §10.5.1 |

#### 10.1.1 尺寸与障碍物几何：按论文 Fig. 2 逐像素实测反推

用户提供的对照图是论文的 **Fig. 2**（不是 Fig. 3），图题是
*"Snapshots of controlled maneuver（red: desired trajectory, blue: actual trajectory）"*，
三个面板分别是 (a) 三维透视 / (b) 俯视 / (c) 侧视。图上的时间刻度
t = 0, 1.66, 3.33, 5, 6.66, 8.33, 10 是**同一个负载的延时快照**，不是多个负载。

在 508×719 px 的原图上逐像素量出的事实：

| 量 | 测量值 | 结论 |
|---|---|---|
| 期望轨迹外接框 | 322 px 宽 × 174 px 高 | **宽高比 ≈ 1.85** |
| 锥在俯视图中的位置 | x 约在轨迹半幅的 ±0.75~0.79 处，y 在竖直中线上 | 锥轴靠近**叶心**，不在 y 极值处 |
| 侧视图锥 | 锥尖 y=31 px，锥底 y=122 px → **高 91 px** | 锥是**正立在地面上**的实体锥 |
| 侧视图负载 | 负载最密处 y ≈ 78 px | 负载飞在**锥高的 52 %** 处 |
| 绳 | 发丝级细线；每个负载 3 根，120° 均布 | 是绳（无弯曲刚度），与本项目一致 |
| 锥在俯视图 | 显示为橙色**圆** | 圆的投影 = 锥底投影，再次确认是锥不是柱 |

据此把三个几何量对齐论文：

| 量 | 旧值 | **新值** | 依据 |
|---|---|---|---|
| `amplitudeY`（y 峰谷差） | 1.20（宽高比 2.50） | **1.62**（宽高比 1.85） | `aY = 2 × 1.50 / 1.85 = 1.62` |
| 锥高 `height` | 0.70 m | **1.05 m** | 负载在锥高 52 % 处 ⇒ `H = 0.55 / 0.52 = 1.05` |
| 锥轴 `(x, y)` | (±1.50, 0.60) | **(±1.50, 0.80)** | 俯视图实测锥在竖直中线附近（叶心） |
| 锥底半径 `radius` | 0.12 m | **0.15 m** | 与增高后的锥保持同一切向斜率比例 |

**改 y 幅值前先做了稳定性扫描**（`_verify_python/_diag_fe_ay.py`），
确认大 y 幅值不会踩到发散悬崖：

```text
   aY     宽高比       结果       环绕均值       环绕峰值        终点
 1.20    2.50       稳定     117.3 mm     587.9 mm     78.7 mm
 1.50    2.00       稳定     128.9 mm     657.2 mm     81.1 mm
 1.62    1.85       稳定     133.7 mm     686.6 mm     82.0 mm     ← 采用
 1.85    1.62       稳定     142.9 mm     744.0 mm     82.9 mm
```

**全部收敛，随 aY 单调平滑劣化，没有任何发散悬崖** —— 所以可以放心对齐论文的 1.85。

改造后实测：环绕均值 117.3 → **133.7 mm**（+16 %，是长高八字的必然代价），
降落终点残差 122.9 → **82.0 mm**（反而变好了），到锥净间隙 165.1 → **210.2 mm**（裕度更大）。

#### 为什么纵向半长要减半（改造 1 的细节）

论文原式 `y = 4.2 cos(0.1πt)` 的**峰谷差是 8.4 m**，而本仿真负载只有 0.20 m、
绳长 0.35 m。照搬这个尺度会让负载与绳索在画面里缩成一个点，摆动也会被极端激发。

同时，"绕两个八字"要求曲线在 τ=0 与 τ=tEnd **都回到同一点**，
而论文原式的自交点并不在 t=0。因此采用**保形平移**（Lemniscate of Gerono）：

```text
pX_bar(τ) = aX * sin(wX τ)                 τ=0 与 τ=tEnd 均为 0
pY_bar(τ) = (aY/2) * (1 − cos(wY τ))       τ=0 与 τ=tEnd 均为 0
```

这是**保形变换**：曲线仍是标准双纽线，只是原点移到自交点、y 幅值从 `aY` 缩到 `aY/2`。
所以配置里 `amplitudeY = 1.62` 表示**纵向半长 0.81 m、峰谷差 1.62 m**。
`tEnd = cycles * 2π / wY = 2 * 20 = 40 s`，而环绕段只有 18 s，
因此时间映射 `τ = (t − T1) * s`，`s = tEnd / T2 = 40/18 ≈ 2.222`。
按链式法则，对真实时间求导要乘 `s`（速度）、`s²`（加速度）。

### 10.2 三段拼接的 C² 连续性

```text
阶段 0  起飞    t ∈ [0, T1)              竖直爬升到环绕平面
阶段 1  环绕    t ∈ [T1, T1+T2)           绕两个完整八字
阶段 2  降落    t ∈ [T1+T2, T1+T2+T3]     水平归位 + 竖直下降到落点
T1 = 3.0 s,  T2 = 18.0 s,  T3 = 4.0 s,  总计 25.0 s
```

起飞段用五次 smoothstep：

```text
σ(u)   = 6u⁵ − 15u⁴ + 10u³      σ(0)=0, σ(1)=1
σ'(u)  = 30u²(1−u)²             σ'(0)=σ'(1)=0
σ''(u) = 60u(1−u)(1−2u)         σ''(0)=σ''(1)=0
```

σ、σ'、σ'' 在两端全为零，所以起飞段在 t=0 与 t=T1 的**位置/速度/加速度**
都与相邻段一致。降落段同理（时间反演）。

环绕段是**闭环**（首尾同点）且端点速度非零，不能靠 smoothstep 单侧归零，
因此用**两端对称包络**：

```text
env(τ) = σ(τ/B) * σ((T2−τ)/B)
env'(τ) = [σ'(τ/B)·σ((T2−τ)/B) + σ(τ/B)·σ'((T2−τ)/B)] / B
env''(τ) = [σ''(τ/B)·σ + 2σ'·σ' + σ·σ''] / B²
```

env 及其一二阶导在 τ=0 与 τ=T2 都为零 → 与 C² 拼接要求相容。

### 10.3 圆锥障碍物：只做可视化与碰撞自检，不进动力学

对应论文 **Fig. 2 / Fig. 3** 的两个锥（Fig. 2 俯视图里它们显示为橙色圆，
即锥底的投影）。参数在 `cfg.obstacles`：

```text
锥位置 (x,y) : (1.50, 0.80) 与 (−1.50, 0.80)   ← 每片叶的叶心（俯视图实测位置）
锥底高度     : 0.00 m（锥底落在地面，锥正立）
锥高 / 底半径: 1.05 m / 0.15 m                 ← 锥尖离地 1.05 m
要求净间隙   : 0.05 m
```

> **为什么锥轴选 (±1.5, 0.8)**：这是每片叶被绕行时轨迹最"远离"的点。
> 注意**不能**直接用 `A_X·sin(·)` 的极值点当锥轴 —— 实际轨迹是
> `startPosition + env(τ)·[p̂(τ) − p̂(0)]`，`startPosition` 的 x 偏移 (+0.10)
> 会把整个八字平移过去，两端包络还会把轨迹收缩。实测（含包络的真实表达式）：
>
> | 候选轴 | 到轨迹最小水平距 |
> |---|---|
> | (±1.5, 0.8) ← 采用（叶心） | **0.3652 m** |
> | (±1.5, 0.6)（旧值） | 0.2599 m |
> | (±1.3, 0.6) | 0.2001 m |
> | (1.45, 0.55) | 0.2912 m（更远但不在叶心，视觉偏移） |

> **为什么障碍物不进入动力学**：论文本身也没有对锥建模（它是"表示为锥的障碍物"，
> 只用于说明绕行意图）。加入排斥力会改变已被证明稳定的闭环，属于另一个问题。
> 因此本项目的做法是：**把轨迹设计成天然绕开锥，并用自检量化间隙**。

> **锥尖必须高于环绕平面**：锥尖离地 1.05 m，而环绕平面离地 0.55 m，
> 余量 0.50 m。若锥尖低于环绕平面，负载就是从锥顶**上方**飞过去的，避障约束形同虚设。
> 这条已写成显式自检项（`锥尖高于环绕平面（避障为真实约束）`）。
>
> 锥高取 1.05 m 的依据：论文侧视图实测负载飞在**锥高的 52 %** 处 ——
> 锥高 91 px（y = 31 → 122），负载最密处 y ≈ 78 px ⇒ 0.52。
> 反解 `H = 0.55 / 0.52 = 1.05 m`，同时满足"负载在锥高一半附近"和"锥尖高于环绕平面"。

间隙判据（`crazyflie_slung_simulation.m` 的 `computeSummary`）：

```text
hBase   = −锥底z                    % 锥底离地高度（单一物理量）
对每个时刻 k、每个锥 j：
    rHoriz  = ‖负载(x,y) − 锥(x,y)‖₂
    hLocal  = (−负载z) − hBase       % 负载相对锥底的离地高度
    若 0 <= hLocal <= 锥高:  radiusHere = 锥底半径 * (1 − hLocal/锥高)
                             gap = rHoriz − radiusHere − 负载外接球半径
                             nInside += 1
    若 hLocal > 锥高       :  gap = +∞（负载在锥顶以上，不可能碰撞）
    否则                   :  gap = rHoriz − 锥底半径 − 负载外接球半径
最终取全程最小 gap，要求 > cfg.obstacles.clearance
★ 有效性门控: obstacleClearanceValid = (nInside > 0)
```

> **为什么用"水平距离"而不是三维点面距离**：锥是竖直放置的回转体，
> 它的外表面完全由"到轴线的水平距离 r(z)"描述（r 随高度线性收缩），
> 这样算最直接、且与锥的几何定义一一对应。

**实测最小净间隙 210.2 mm**（在 t = 10.47 s，即环绕段中段），
远大于 50 mm 判据 → PASS，且 `nInside = 23582` 点说明避障**确实被检查过**。

> **★ 210.2 mm 与 §10.3.2 里解析估算的 152.0 mm 为何不同**（这个差值我专门核对过，
> 不是算错）：解析估算取的是**期望参考轨迹**上的最近点（dmin = 0.3652 m），
> 而仿真实测取的是**负载实际轨迹**。最小间隙时刻负载实测位置是
> `[−1.3304, 0.4131]`，水平距锥轴 **0.4225 m** —— 比参考轨迹**远 0.0573 m**。
> 原因是控制器在八字腰部存在约 0.06 m 的跟踪滞后，而滞后方向恰好**朝外**（远离锥轴）。
> 所以 210.2 mm 是**真实的物理裕度**，比解析下界还宽，两个数都对。

#### 10.3.2 改几何后的净间隙解析核对

锥轴 (±1.50, 0.80)、锥高 1.05 m、锥底半径 0.15 m，负载飞在 0.55 m 高：

```text
该高度处锥半径 r(0.55) = 0.15 × (1 − 0.55/1.05) = 0.0714 m
参考轨迹到锥轴最近距离 dmin = 0.3652 m
负载外接球半径 = 0.5 × ‖[0.20, 0.20, 0.020]‖ = 0.1418 m

净间隙 = 0.3652 − 0.0714 − 0.1418 = +152.0 mm  =  判据 50 mm 的 3.0 倍
```

> 新几何的 dmin（0.3652 m）比旧几何（0.2599 m）**还大**：因为锥轴从 y=0.60
> 移到叶心 y=0.80 后，正好落在一个**离轨迹更远**的凹陷处。
> 所以这次"对齐论文"同时改善了**保真度**和**安全裕度**，没有取舍。

#### 10.3.1 ★★ 一个把自检整个废掉的静默失效（已修复）

这是本项目**最危险**的一类错误：不报错、结果还很好看、判据照样 PASS，
但实际上是"**根本没检查**"。

**故障现象**：旧参数下自检报告"最小净间隙 560.9 mm，PASS"。
看起来完美，但那个 560.9 mm 出现在 **t = 0.41 s** —— 起飞刚起步、
负载还在地面附近的时候，**与环绕段毫无关系**。

**根因（三重）**：

| # | 问题 | 说明 |
|---|---|---|
| 1 | 锥体倒插 | 旧值 `baseZ = −0.35`（离地 0.35 m）、`height = 0.30`，而可视化把"高"往**下**画 ⇒ 锥尖扎进地面以下，**整个锥体都位于环绕平面（离地 0.55 m）的下方** |
| 2 | 公式语义混乱 | 旧式 `hAboveBase = (baseZ − z) + height` 再与 `height` 比较，实际量是"离**锥顶**的距离"而非"离**锥底**的高度"。锥长期在负载平面以下时它恒 `> height` ⇒ 全程走 `gap = +∞` 分支 |
| 3 | 几何穿模 | 旧轴 (0.75, 0.60) 到轨迹只有 **0.0288 m**，小于该高度处锥半径 + 负载外接球（−0.1833 + 0.1418）⇒ 路径**穿过**锥体 |

**量化证据**（`_verify_python/_test_obstacle_clearance.py` 复现）：

```text
旧公式 + 旧几何:
  区间内采样 = 524 | 锥顶以上(inf) = 22978 | 锥底以下 = 1500
  有效采样占比 = 2.1%   （而环绕段占全程 72%！）
  环绕段区间内采样 = 0   ← 关键：环绕段从未被检查
  旧报告的 min 净间隙 = 564.6 mm ≈ 旧报告里的 560.9 mm（同一来源）

新公式 + 新几何:
  区间内采样 = 23582 | 锥顶以上(inf) = 0 | 锥底以下 = 1420
  环绕段区间内采样 = 18000
  真实 min 净间隙 = 152.0 mm（对参考轨迹） / 210.2 mm（对实际轨迹）
```

> ★ **教训**：这类失效**不能靠"全程是否采样过"来发现** —— 起飞段必然采样到，
> 全程计数不为 0。门控必须针对**真正关心的那一段**（这里是环绕段）。
> 已加入两项永久防线：
> 1. `summary.obstacleBranchCounts` 记录三个分支各走了多少次，直接暴露"恒走 inf"；
> 2. `summary.obstacleClearanceValid = (nInside > 0)`，**零采样即判无效**，
>    不再让"没检查"伪装成"通过"。
> 3. 新增自检项 `锥尖高于环绕平面（避障为真实约束）`，防止几何退化再次发生。

> ★ **另一个教训**：穿模判据必须用**负载所处高度处的锥半径**，而不是锥底半径。
> 负载只在 0.55 m 高度飞行，锥在那里已收缩到 0.0714 m，若误用锥底半径 0.15 m
> 会得到"穿模"的**假结论**（需 > 0.2918 m vs 实际 0.2132 m）。回归测试里
> 把这个陷阱也固化下来了。

### 10.4 三维可视化：期望路径 + 两个锥

`crazyflie_slung_visualization.m` 新增三个函数：

| 函数 | 作用 |
|---|---|
| `drawObstacles(ax, cfg)` | 用 24 边**棱锥**近似圆锥。★ 面片必须显式构造（顶点 + 底面圆环 + 三角面索引矩阵），不能像折线那样首尾拼接 —— 棱锥的顶点参与每个三角面，拼接顺序写错会画成一团乱麻 |
| `drawDesiredPath(ax, cfg)` | 期望八字（灰色虚线）+ **环绕段单独用绿色实线强调**（让"绕八字"一眼可见）+ 起点圆点 + 环绕段入口方块 |
| `referencePathPoints(cfg)` | 采样期望轨迹，供坐标轴范围计算使用 |

并修掉一个会随八字工况暴露的坐标轴缺陷：

> **坐标轴范围不能只看"起点 + 目标 + 最小跨度"**。
> 八字工况下前 3 s 是原地起飞，用前 3 s 的点定轴框会把它定得很小，
> 等负载飞到 ±1.5 m 时四旋翼直接跑出画面。
> 因此轴框改为纳入 **期望轨迹本身**与 **障碍物外沿**。

### 10.5 ★★★ 两个根因：为什么原参数在八字工况下必然发散 ★★★

这是本项目**最难的一段调试**。现象：把静态悬停调好的参数直接用到八字工况，
闭环在 **t = 3.76 s 发散**（正好是起飞段刚结束、环绕段刚起步的时刻）。

排查过程（共 11 个诊断脚本 `_diag_fe_*.py`）逐一**排除**了：
Ad 前馈缺失、姿态环带宽、KX/KV 增益、巡航速度/时长、振幅、
负载偏航通道、偏航阻尼、挂点几何、KQ/KW、仿真步长、包络阶次（C³）、
位置环带宽。最终定位到**两个独立的根因**，且**缺一不可**。

#### 10.5.1 根因一：两端对称包络在出口处制造加速度尖峰

把环绕段参考写成：

```text
p_d(t) = startPos + env(τ) * [p̂(τ) − p̂(0)]
```

对时间求二阶导（`s = tEnd/T2`）：

```text
a(t) = env'' · p̂ + 2 · env' · s · v̂ + env · s² · ĉ
```

其中 `env'' ~ 1/B²`。当 B 很小时，`env''` 在 τ ≈ B 处（包络**出口**）达到极大，
而那里恰好也是 `s²·ĉ` 接近峰值的位置，**两项同相叠加**：

```text
参考加速度理论峰值（纯八字）          = 2.92 m/s²
B = 1.2 s 时的实际参考加速度峰值      = 9.20 m/s²      ← 放大 3.15 倍
```

尖峰出现在 **t = 3.7 ~ 4.2 s**（包络窗口 1.2 s 的尾端），正好对上发散时刻 3.76 s。
位置环 KX = 3.0 要求 `9.2 / 3.0 ≈ 3 m` 的稳态偏移才能平衡这个尖峰，
而绳长只有 0.35 m —— **系统必然发散**。

**解法**：放大倍数 ∝ `1/B²`，把 B 从 1.2 s 拉到 **6.0 s**（抑制 25 倍）即可：

```text
B = 1.2 s  ->  t = 3.76 s 发散
B = 6.0 s  ->  完整跑完 25 s 不发散
B ∈ [5, 7] 均收敛，取 6.0（给包络留出 1/3 的环绕段时长）
```

> **为什么不用七阶（C³）smoothstep 代替五阶**：实测只能把误差从 138 mm 降到 88 mm
> （`_diag_fe_jerk.py`），因为问题出在 **`env''` 的幅值**而不是它的连续性。
> 所以正解是**加长 B**，不是提高阶次。这是一个容易走错的方向。

#### 10.5.2 根因二：`q_id_dot` 的差分数值噪声

论文 (27) 式的大括号里有 `−(q_i · ω_id) · q_id_dot` 项，其中
`ω_id = q_id × q_id_dot`。而 `q_id` 是按 (25) `q_id = −μ_id/‖μ_id‖` 算出的**单位向量**。

对它做一阶差分再除以 `dt`，本质是"两个单位向量之差 / 0.002 s"，信噪比极差；
而且 `q_id` 会随负载的微小摆动在**单位球面上抖动**，差分出来的幅值远大于物理真值。
这个被污染的 `q_id_dot` 经 `ω_id` 进入 (27)，构成一条**正反馈**通道。

逐项隔离实验（`_diag_fe_terms.py`，aX = 0.2 m）给出了决定性证据：

```text
保留原式                          ->  20.75 s 发散
去掉 −(q_i·ω_id)·q_id_dot 项      ->  全程稳定（|ex| 峰 138 mm）
单独去掉 ω_id 前馈                ->  结果完全一致（该项是唯一来源）
```

**解法**：`USE_QID_DOT = false`，即求解 `q_id_dot` 但**不把它送进 correction**。
稳定域一举扩大 5 倍：

```text
USE_QID_DOT = true   ->  稳定域只到 aX ≈ 0.15 m（仍会发散）
USE_QID_DOT = false  ->  稳定域扩到 aX ≈ 0.80 m 以上
```

> 这与静态工况下"该项理想值为 0"是一致的：静态悬停时看不到差别，
> **只有八字这种 `q_id` 持续变化的工况才会把噪声放大出来**。
> 代码里保留了这个开关（默认 false），置 true 可复现论文原式的发散现象。

#### 10.5.3 两个根因的相互关系

两者**独立且缺一不可**：

```text
B=1.2 + QIDDOT=on   ->  t =  3.76 s 发散（包络尖峰主导）
B=6.0 + QIDDOT=on   ->  t = 13.12 s 发散（qid_dot 噪声主导）
B=1.2 + QIDDOT=off  ->  t ≈  5 s   发散（包络尖峰仍然致命）
B=6.0 + QIDDOT=off  ->  全程 25 s 稳定  ← 正确组合
```

只解决一个只能推迟发散时刻，必须两个都改。

### 10.6 位置增益重标定（绕八字专用）

静态悬停用 `[3.0; 3.0; 3.75]` 就够（误差 3.63 mm），但绕八字时参考点持续运动，
位置环必须"追得上"，否则留下相位滞后型跟随误差。实测扫描
（`_diag_fe_tune4.py` / `_diag_fe_tune5.py`，A = ±1.5 m，B = 6.0）：

```text
 KX      wn=√KX       相对绳摆频率(5.29 rad/s)   环绕段误差均值   末点残差   稳定性
  3.0    1.73 rad/s    0.33 ×                     287.0 mm       104.7 mm   稳定
  5.0    2.24 rad/s    0.42 ×                     216.9 mm       130.9 mm   稳定
  8.0    2.83 rad/s    0.53 ×                     162.7 mm       103.3 mm   稳定
 10.0    3.16 rad/s    0.60 ×                     142.8 mm        96.4 mm   稳定
 14.0    3.74 rad/s    0.71 ×                     133.7 mm        82.0 mm   稳定 ← 采用
 16.0    4.00 rad/s    0.76 ×                     112.4 mm        73.9 mm   稳定
 20.0    4.47 rad/s    0.84 ×                     ——             ——         11.10 s 发散
 24.0    4.90 rad/s    0.93 ×                     ——             ——          3.83 s 发散
 28.0    5.29 rad/s    1.00 ×（匹配）             ——             ——          2.42 s 发散
 40.0    6.32 rad/s    1.19 ×                     ——             ——          1.34 s 发散
```

> 表中 KX = 14 那一行的误差数字是 **aY = 1.62 下重测的**（133.7 / 82.0 mm）；
> 其余各行仍是 aY = 1.20 时扫出来的，用于展示**趋势与悬崖位置**。
> 悬崖位置（KX ≥ 20 发散）在 aY = 1.62 下未变。

> ### ★ 一个反直觉的结论：让位置环带宽"匹配"绳摆频率反而最快发散
>
> 纯直觉会说"带宽匹配才有最好的能量传递"。但这里的耦合是**寄生**的：
> 位置环越快，越会把负载的水平运动"硬"地转成绳索摆动，
> 而绳向环（kq = 55）跟不上这个加快的激励，摆角迅速积累到翻转发散。
>
> **最优带宽是绳摆频率的 0.6 ~ 0.7 倍**（约 −3 dB 处仍能跟随，但不与摆模态共振），
> 而不是 1.0 倍。这个结论与常见的"外环带宽要尽量高"的经验相反。

**边界很陡**：14 → 20 之间只有一步之差就发散。因此 KX = 14 是
"贴着边界取"的最优值，靠下面三项裕度守住：

```text
a. blendTime = 6.0 s       （消除参考加速度尖峰，§10.5.1）
b. USE_QID_DOT = false     （去掉 q_id_dot 差分噪声，§10.5.2）
c. KV/KX = 0.62            （zeta ≈ 1.16，给摆模态留阻尼裕度）
```

**KV 的取法**：闭环二阶特性 `wn = √KX`，`zeta = KV / (2√KX)`。
`0.62 × 14.0 = 8.68` 给出 `zeta ≈ 1.16`（略过阻尼）。
阻尼细扫（`_diag_fe_tune5.py` 24c）确认 `KV/KX` 在 0.62 附近最稳，
偏离到 0.50 或 0.90 都提前发散。

**ki 同步从 1.60 提到 3.00**：KX 提高后同样的 ki 显得偏软，
末点残差从 123 mm 降到 82.0 mm（aY = 1.62 下重测）。`ki >= 6.0` 会让积分在环绕段的往复激励下发散
（`_diag_fe_tune3.py` 实测 KI = 6.0 于 11.30 s 发散）。

### 10.7 最终参数集与稳健性

**最终参数集**（已写入 `crazyflie_slung_parameters.m` 与 Python 镜像）：

```text
cfg.figureEight.amplitudeX      = 1.50        % x 半宽  [m]
cfg.figureEight.amplitudeY      = 1.62        % y 峰谷差（半长 0.81）[m]，宽高比 1.85 对齐论文
cfg.figureEight.omegaX          = 0.2*pi      % 论文原值
cfg.figureEight.omegaY          = 0.1*pi      % 论文原值
cfg.figureEight.cycles          = 2.0         % 两个完整八字
cfg.figureEight.takeoffDuration = 3.0         % [s]
cfg.figureEight.cruiseDuration  = 18.0        % [s]
cfg.figureEight.landingDuration = 4.0         % [s]
cfg.figureEight.cruiseHeight    = -0.55       % 环绕平面高度 [m]
cfg.figureEight.blendTime       = 6.0   ★     % 包络过渡时长 [s]
cfg.loadController.kx           = [14.0; 14.0; 17.5]   ★
cfg.loadController.kv           = [8.68; 8.68; 8.68]   ★
cfg.loadController.ki           = [3.00; 3.00; 3.00]   ★
USE_QID_DOT = false                     ★     % 在 controller.m 里
```

**±20% 稳健性验证**（`_diag_fe_final.py` 25b，全部仍稳定）：
`B ∈ {5.0, 7.0}`、`KX ∈ {12, 16}`、`INT_LIM ∈ {0.4, 0.6}`、`C1 ∈ {0.4, 0.6}`
—— 12 个扰动工况**全部完整跑完 25 s**，环绕段均值在 112 ~ 125 mm 之间，
末点残差在 62 ~ 99 mm 之间。说明参数不是"卡在刀刃上"的脆弱解。

**逐秒误差剖面**（`_diag_fe_final.py` 25a，摘录）：

```text
   t     |ex|mm |  期望x    实际x  |  期望y    实际y  |  期望z    实际z
  0.0      0.0 |  +0.100   +0.100 |  -0.060   -0.060 |  +0.450   +0.450
  1.5     13.6 |  +0.050   +0.041 |  -0.030   -0.020 |  -0.050   -0.050
  3.0      1.3 |  +0.000   +0.001 |  +0.000   +0.001 |  -0.550   -0.549   ← 起飞收尾极干净
  8.0     96.2 |  +0.930   +1.026 |  +1.515   +1.514 |  -0.550   -0.551
 11.0     24.7 |  -1.477   -1.502 |  +0.190   +0.188 |  -0.550   -0.554
 13.5      2.7 |  +1.299   +1.299 |  +0.405   +0.407 |  -0.550   -0.549   ← 误差瞬时归零
 18.0    686.4 |  +0.650   +1.089 |  +0.607   +1.135 |  -0.550   -0.545   ← 最大误差
 21.0    125.4 |  +0.000   -0.006 |  +0.000   +0.125 |  -0.550   -0.550
 25.0     82.0 |  +0.000   +0.023 |  +0.000   +0.079 |  -0.350   -0.350   ← 落点收尾
```

> 注意 t = 8.0 s 与 18.0 s：期望 y 已经加到 1.515 / 0.607（旧 aY=1.20 时是 1.123 / 0.450），
> 这是加高八字后的直接体现；峰值误差也从 587.9 涨到 686.4 mm，但仍在可控范围。

三阶段的误差特征各不相同，这也是为什么要**分段统计**而不是只看总指标：

| 阶段 | 误差峰值 | 特征 |
|---|---|---|
| 起飞 [0, 3) | **23.4 mm** | 近乎完美。竖直爬升参考加速度小，位置环轻松跟上 |
| 环绕 [3, 21) | 686.6 mm（均 133.7 mm） | 误差**在环绕段末端（t ≈ 18 s）集中爆发**，因为那时参考轨迹的往复最快（`s²·ĉ` 最大），绳摆相位滞后最严重 |
| 降落 [21, 25] | 125.4 mm | z 通道**极干净**（≤ 3 mm），水平残差主要是环绕段收尾时残留的 y 偏移（125 mm → 79 mm 衰减） |

**关于末点残差 82.0 mm 的诚实说明**：它主要是 y 方向残留，来自两个因素
（a）环绕段在 t=21 s 结束时正好留下约 125 mm 的 y 偏移；
（b）降落段只有 4 s，不足以把它完全积分掉。
若要进一步收紧，可延长 `landingDuration` 到 6~8 s，或在降落段加大 ki。
本仿真保持 4 s 以符合用户指定的时间分配。

### 10.8 自检如何适应八字工况（两组判据）

`crazyflie_slung_demo.m` 现在按工况分两组判定：

**通用组**（两种工况都检查，与之前相同）：
分配矩阵秩、张力分配残差 ×2、状态有限性、绳向收敛、三机构型、
张力正性与数值、绳索长度不变量、推力裕度、角速度范围、偏航漂移率。

**八字组**（仅 `cfg.referenceFcn` 非空时检查）：
起飞段误差 < 50 mm、环绕段峰值 < 1.0 m、环绕段均值 < 0.25 m、
降落段 < 0.20 m、降落终点残差 < 0.10 m、x 方向完成 2×cycles 次往复、
**到锥净间隙 > 规定值**、**锥形障碍物净间隙自检有效（非静默失效）**、
**锥尖高于环绕平面（避障为真实约束）**、
**blendTime >= 5 s**（防止将来有人为了"贴近论文"
把它调小，那会直接导致发散）。

> **★★ 关于"净间隙自检有效"这一条**：它专门用来防住 §10.3.1 描述的静默失效。
> 净间隙的**数值**判据（`> 规定值`）本身并不能证明避障被检查过 —— 旧版正是
> 因为环绕段恒走 `gap = +∞` 分支，用一个起飞瞬间的值骗过了判据。
> 所以必须**同时**断言 `obstacleClearanceValid`（落在锥高度区间内的采样点数 > 0），
> 并配合 `锥尖高于环绕平面` 防止几何退化。三条一起才能保证"避障真的成立"。

> **注意两条判据被有意"降级"为仅静态工况**：
> - `稳态位置误差 < 2 cm`：八字工况下"稳态"这个概念不成立（参考点一直在动），
>   硬套 2 cm 会得到"永远 FAIL"的假告警，反而掩盖真正的跟踪问题；
> - `负载终位置 < 0.02 m`：八字工况改用 `降落终点残差 < 0.10 m`，
>   因为降落段只有 4 s 来消化环绕段的残余误差。

### 10.9 本节的诊断脚本清单

全部位于 `_verify_python/`，可复现本节的每一条结论：

| 脚本 | 回答的问题 |
|---|---|
| `_diag_fe_isolation.py` | 逐方向隔离：**x 方向机动是致命的，y 方向稳定** |
| `_diag_fe_rho.py` | 挂点几何是不是原因？→ **0/7 收敛，不是** |
| `_diag_fe_freeze.py` | 冻结负载偏航能救吗？→ **不能（3.81 s 仍发散），偏航是症状不是根因** |
| `_diag_fe_terms.py` | 逐项关闭 (27) 式 → **定位到 `q_id_dot` 项是唯一来源** |
| `_diag_fe_qiddot.py` | `USE_QID_DOT` 开关的稳定域 → **5 倍提升** |
| `_diag_fe_jerk.py` | 七阶（C³）包络能救吗？→ **只能小幅改善，不是正解** |
| `_diag_fe_bw.py` | 提高位置带宽能救吗？→ **反而更早发散** |
| `_diag_fe_blend.py` | **包络时长扫描 → 找到 B=6.0 这个解** |
| `_diag_fe_tune4.py` | 干净的 KX/KV/KI 重扫（修正了早期一次状态泄漏） |
| `_diag_fe_tune5.py` | **KX 上推到边界 + 阻尼细扫 → 定出 KX=14/KV=8.68** |
| `_diag_fe_final.py` | **最终参数集验证 + ±20% 稳健性 + 逐秒剖面** |
| `_diag_fe_ay.py` | ★ **y 幅值扫描（对齐论文宽高比用）**：aY ∈ {1.20, 1.35, 1.50, 1.62, 1.70, 1.85} **全部稳定、单调平滑劣化、无悬崖** ⇒ 可以放心用论文的 1.85 宽高比。★ 同时演示了"每个算例先重置确定性基线"的正确写法（每次重新 `exec` 一遍模块源码），避免状态泄漏 |
| `_test_obstacle_clearance.py` | ★ **锥间隙自检的回归测试**：复现旧的静默失效（环绕段有效采样 = 0）、验证新公式给出真实净间隙、核对有效性门控与"锥尖高于环绕平面"、拆穿"误用锥底半径会得到穿模假结论"这个陷阱 |

> **调试教训（值得记下）**：`_diag_fe_tune3.py` 的 22b 一节曾给出"提高 KX
> 会让系统提前发散"的**错误结论** —— 原因是探针函数的 `saved/restore`
> 逻辑没有在每轮之间重置中间状态，导致后续用例继承了上一轮的值（状态泄漏）。
> 修正为"每个用例先无条件写回已知基线、再叠加覆盖"后，真实结论完全相反：
> **提高 KX 单调改善跟踪精度**，边界在 14 与 20 之间。
> 教训：**扫描脚本必须保证每个用例的初始状态完全确定**，
> 否则得到的"趋势"可能是伪影。现在 `_diag_fe_tune4.py` / `_diag_fe_tune5.py` /
> `_diag_fe_final.py` 都采用这种"无条件重设基线"的写法。



---

## 11. 用户第二轮改动：挂点移到物品边缘 + 八字两叶**真正环绕**锥

用户提出两点几何要求，两点都改了，而且**两点都必须改轨迹/参数**才能成立：

> 1. 无人机绳索的固定点位置需要调整：不要放在物品中间，而是改为在物品边缘设置
>    三个固定点，其中一个位于某条边的中点，另外两个位于相对边的两个顶点处。
> 2. 八字形的两个圆心应位于圆锥的中心，并围绕该中心进行环绕。

### 11.1 挂点：从"内接等边三角形"改为"一边中点 + 对边两顶点"

负载上表面是边长 0.20 m 的方形，取 z 向下的负载体系：

```text
挂点 1 = ( +0.10,  0.00, -0.010)   ← x = +0.10 那条边的**中点**
挂点 2 = ( -0.10, +0.10, -0.010)   ← x = -0.10 那条边的一个**顶点**
挂点 3 = ( -0.10, -0.10, -0.010)   ← x = -0.10 那条边的另一个**顶点**
底边长 0.20 m，腰长 sqrt(0.20^2 + 0.10^2) = 0.2236 m
```

**两个后果（都已数值核算）**：

| 量 | 旧（内接等边 r=0.085） | 新（边中点 + 对边顶点） |
|---|---|---|
| `rank(P)` | 6 | **6**（仍可解） |
| `cond(P P')` | 276.82 | **150.20**（力臂更长，条件数变小 ⇒ 分配更良态） |
| 悬停张力 | [0.2616, 0.2616, 0.2616] N | **[0.3924, 0.1962, 0.1962] N（2:1:1）** |
| 推力占比 | [43.5, 43.5, 43.5] % | **[53.3, 38.6, 38.6] %** |

> **★ 为什么张力变成 2:1:1**：挂点质心在 (−0.0333, 0)，相对负载质心偏了 0.0333 m。
> 悬停时各绳 μ_i 必须同时满足力平衡 Σμ = m0 g 与力矩平衡 Σ ρ̂_i μ_i = 0。
> 挂点质心与负载质心重合时才三等分；偏心后解出 μ_1 = 2μ_2 = 2μ_3。
> **这是几何决定的，不是可选参数** —— 自检里已把这条写成
> "稳态张力 = 挂点几何决定的悬停解"（联立上面两个方程解出理论值再与实测比对）。

### 11.2 ★★ 新挂点会让闭环发散，必须提高机体姿态环增益

改完挂点后**连静态悬停都在 t = 1.202 s 发散**（旧挂点稳定）。这不是轨迹问题：

```text
旧 RHO | 静态悬停: |ex|峰值 808.455 mm  张力 [0.2003, 0.7936] N  未发散
旧 RHO | 八字   : |ex|峰值 686.633 mm  张力 [0.0985, 0.4172] N  未发散
新 RHO | 静态悬停: *** 发散于 k=601  t=1.202s                    张力爆到 1.2e7 N
新 RHO | 八字   : *** 发散于 k=823  t=1.646s                    张力爆到 1.8e10 N
```

**根因**：2:1:1 的静载意味着每架机受到的**绳索反作用力不再对称**，
机体必须更"硬"地保持姿态，否则推力方向偏离 → 与绳摆环形成正反馈。
分配矩阵本身没问题（`cond` 反而变小，悬停 `q_id` 全竖直）。

**修法**：提高 `cfg.attitudeController.kR`。实测倍率扫描（`_diag_attach_retune.py`）：

| kR 倍率 | (roll, yaw) 增益 | 静态悬停 | 八字工况 |
|---|---|---|---|
| ×1 | (30, 15) | 发散 1.202 s | 发散 1.646 s |
| ×2 | (60, 30) | 稳定 15.25 mm | 发散 7.290 s |
| ×3 | (90, 45) | 稳定 15.11 mm | 发散 10.200 s |
| ×5 | (150, 75) | 稳定 14.69 mm | **稳定** 692.3 mm |
| **×8** | **(240, 120)** | 稳定 **14.65 mm** | **稳定 693.5 mm ← 采用** |
| ×10 | (300, 150) | 稳定 14.67 mm | 稳定 693.3 mm |
| ×20 | (600, 300) | 稳定 14.67 mm | 稳定 695.5 mm |

悬崖在 ×3 与 ×5 之间；**×5 ~ ×20 是性能几乎不变的平台区**，取 ×8 居中留裕度。
（增益再大时 `maxAttitudeError` 与 `maxBodyRateCommand` 的限幅生效，
所以平台区很宽 —— 这也说明这不是"卡在刀刃上"的脆弱解。）

> ★ 这不是"用增益压住问题"：绳索反作用不对称是真实的物理扰动，
> 提高机体姿态环带宽是**对应的、正确的**处置。

### 11.3 ★★★ 原轨迹的"两叶"根本不闭合 —— 谈不上环绕

用户要求"围绕叶心环绕"，先得回答：**现在的轨迹到底有没有围着叶心绕？**

八字的形状（先量清楚，不能再推测）：

```text
环绕段 p_d = [ aX·sin(ωX τ)·env , (aY/2)(1 - cos(ωY τ))·env , zC ]   以原点为中心
ωX = 2·ωY  ⇒ x 是**倍频**轴 ⇒ 两片叶**沿 y 上下堆叠**
自交点在 (0, aY/2) = (0, 0.81)；上叶 y∈[0.81,1.62]、下叶 y∈[0,0.81]，均横跨 x∈[-1.5,1.5]
```

**判定"被包围"用射线-线段求交**（不是射线-点）。这里踩过一个坑：

> ★★ 第一版判定写成"射线到轨迹**采样点**的垂距 < 4 mm 算命中"，
> 而采样点间距约 50 mm，射线几乎必然从两点之间穿过 ⇒ **恒判"未包围"**。
> 这是**测试自身的 bug**，一度让我以为"轨迹无解、必须换形状"。
> 正解：做**射线-线段**求交。

用正确的判定量出来的结果（`_diag_fig8_lobes.py` / `_diag_orbit_period.py`）：

| T2 | B | 包络满幅窗口 τ | 上叶 | 下叶 | 到轨迹最小距 | 净间隙 |
|---|---|---|---|---|---|---|
| 18 | 6.0 | [12.7, 27.3] | **无包围点** | 有 | — | — |
| 18 | 5.0 | [10.6, 29.4] | 有 | 有 | 0.0419 m | **−171.3 mm FAIL** |
| 30 | 5.0 | [6.4, 33.6] | 有 | 有 | 0.2962 m | +83.0 mm PASS |
| **45** | **5.0** | [4.2, 35.8] | 有 | 有 | 0.3913 m | **+178.1 mm PASS ← 采用** |
| 45 | 6.0 | [5.1, 34.9] | 有 | 有 | 0.3910 m | +177.8 mm PASS |

**根因**：环绕段的包络 `env(t) = σ(t/B)·σ((T2-t)/B)` 只在**中段**等于 1，
而"两个八字"意味着整个八字要在环绕段里被描出**两次**（τ 从 0 走到 40）。
于是八字是在"生长 / 收缩"途中被描出的 —— **实际轨迹是螺旋，两片叶不闭合**。
包络满幅窗口长度 = `T2 - 2B`，必须撑到覆盖住 τ∈[5,35]（两叶各完整遍历一次）。

**修法**：`cruiseDuration` 18 → **45 s**，`blendTime` 6.0 → **5.0 s**（B 取下限）。

> ★★ **这个改动让系统更稳，不是妥协**：时间映射 `s = 40/T2` 从 2.222 降到 0.889，
> 而参考加速度 ∝ s²，于是**参考加速度降到原来的 16%**。
> "让轨迹真正环绕"与"稳定性"两个目标方向一致。
> 实测环绕段误差均值从 133.7 mm 降到 **43.3 mm**。

### 11.4 叶心怎么定 + 最终锥位

叶心定义：**在实际轨迹上求"被轨迹包围 且 到轨迹最小距离最大"的点**（最大内切圆心）。

> ★ 不能用**理想八字**（env≡1）的叶心：包络把路径向原点收缩，实际顶点只有
> (0, 1.4523) 而不是 (0, 1.62)，理想叶心 (0,1.215) 到实际轨迹只剩 0.237 m
> ⇒ 净间隙仅 24 mm，不达标。**必须用含包络的实际轨迹来定心。**

实测（T2=45, B=5）：

```text
上叶心 (-0.0187, +1.2252) -> 到轨迹最近 0.3947 m -> 净间隙 +181.5 mm
下叶心 (-0.0187, +0.3948) -> 到轨迹最近 0.3938 m -> 净间隙 +180.6 mm
两锥间距 0.8305 m
```

最终：

```matlab
cfg.obstacles.positions = [-0.0187, -0.0187; 1.2252, 0.3948];   % 每列 = [x; y]
```

**两个锥在俯视图里是上下排列、位于同一条竖直线上**（因为两片叶沿 y 堆叠），
负载在它们之间穿过自交点、分别绕两片叶各飞两圈。
这与论文 Fig. 2 俯视图"两锥左右并排"是同一种构型的两个视角 —— 论文里 y 是长轴
（幅值 4.2 vs x 的 1.2），显示时把 y 横过来放，就变成左右并排。

> ★ 若希望**在本仿真里也显示成左右并排**，需要把"低频轴"换成 x
> （即 `ωX = 0.1π`、`ωY = 0.2π`）。但那会让 `p̂y = (aY/2)(1-cos 2ωτ) = aY·sin²(ωτ) ≥ 0`，
> 退化成一条**抛物线**（来回描同一段），不再是八字。
> 正确做法是改用经典 Lissajous 形式 `p̂y = (aY/2)·sin(2ωτ)`。
> 这是个**轨迹形式的改写**，会影响已标定的增益，本次未做（用户未要求）。

### 11.5 ★ 初始推力也必须按挂点几何给（否则起步瞬间推力饱和）

旧代码 `cfg.initial.thrustNewton = (m0/3)g + mg = 0.5804 N`（各机相同）。
新几何下正确的悬停推力是 **[0.7112, 0.5150, 0.5150] N** —— 用旧值时机 1 被低估 0.13 N，
而机体姿态环立刻要纠偏，实测**起步瞬间推力打到 100%（饱和）**。

修法：由挂点几何解出悬停张力再算推力，并把 `thrustNewton` 从标量改成 1×n 向量
（`crazyflie_slung_simulation.m` 里加了标量/向量两种输入的兼容）。

### 11.6 最终实测（Python 镜像，全程 52 s）

```text
起飞段 [0, 3.0 s]      : 峰值     18.5 mm
环绕段 [3.0, 48.0 s]   : 峰值    705.0 mm / 均值     43.3 mm
降落段 [48.0, 52.0 s]  : 峰值    183.9 mm
降落终点残差           :     90.1 mm
轨迹行程 (x, y)        : (3.002, 1.681) m
包络过渡时长           : 5.0 s
到锥最小净间隙         :    125.3 mm (t = 51.76 s)  [PASS, 判据 > 50 mm]
  自检有效性           : [有效]（锥高度区间内 50582 点 / 锥顶以上 0）
  锥尖离地 / 环绕平面  : 1.05 m / 0.55 m（锥尖更高 ⇒ 避障是真实约束）
稳态各绳索张力         : [0.3924, 0.1963, 0.1964] N
理论悬停张力（几何解）  : [0.3924, 0.1962, 0.1962] N
绳索长度不变量         : 2.22e-16 m  PASS
最小张力裕度           : 33.0 %（相对最小那根绳的悬停张力）
推力峰值占比           : 69.1 %
最大机体角速度         : 3.938 rad/s
状态有限 / 各机在负载上方: True / True
```

**静态悬停工况**（`cfg.referenceFcn = []`）：稳态位置误差 **12.72 mm**、
终端残差 **1.89 mm**（初始推力按几何给之后，从 10.05 mm 显著改善）、
稳态张力 [0.3924, 0.1962, 0.1962] N 与理论**完全一致**、
稳态绳向误差 0.379°、roll/pitch 姿态 0.000°、偏航漂移率 0.366 rad/s。

### 11.7 ★ 诚实说明：两处保留的裕度偏紧

| 项 | 实测 | 判据 | 裕度 |
|---|---|---|---|
| 降落终点残差 | 90.1 mm | < 100 mm | 仅 10% |
| 负载偏航漂移率 | 0.862 rad/s | < 1.0 rad/s | 仅 14% |

- **降落终点残差** 主要是 x 方向残留（终位置 x = −83.8 mm），受 4 s 降落时长限制
  （用户指定）。加长到 6~8 s 可进一步收紧。**实测把 `ki` 从 3.0 提到 4.0/5.0/6.0
  反而更差**（108.1 / 121.7 / 128.7 mm），8.0 在 23.1 s 发散 ⇒ 保持 `ki = 3.0`。
- **偏航漂移率**随运行时长增长：静态 30 s 是 0.366 rad/s，八字 52 s 是 0.862 rad/s。
  新挂点几何让负载偏航与重力力矩的耦合更强（挂点质心绕质心旋转），
  这是**物理后果**，不是控制器缺陷（偏航是欠驱动自由轴，见 §5.1）。
  若要把演示拉得更长，这一项会先失效。

### 11.8 本节新增/改动的文件

| 文件 | 改动 |
|---|---|
| `crazyflie_slung_parameters.m` | 挂点改为边中点+对边顶点；`kR` ×8；`cruiseDuration` 45；`blendTime` 5.0；`duration` 52；`obstacles.positions` 改为两叶心；初始推力按几何解改为向量 |
| `crazyflie_slung_simulation.m` | `thrustNewton` 兼容标量/向量输入 |
| `crazyflie_slung_demo.m` | 张力自检改为"= 挂点几何决定的悬停解"；推力判据区分起步/稳态；quick 模式的时长不再硬编码 |
| `_verify_python/_multi_mirror.py` | 同步全部改动；新增"理论悬停张力（几何解）"输出 |
| `_verify_python/_test_obstacle_clearance.py` | 新增 **F 节：环绕性核对**（射线-线段求交）；修正自身两处轨迹重建错误（见下） |
| `_verify_python/_diag_fig8_lobes.py` | 新建：叶心求解 + 环绕性判定 |
| `_verify_python/_diag_orbit_period.py` | 新建：T2/B 扫描，找"满幅窗口覆盖整个叶"的解 |
| `_verify_python/_diag_attach_retune.py` | 新建：新挂点几何下的增益扫描 |
| `_verify_python/_sweep_blend_orbit.py` / `_sweep_ay_orbit.py` | 新建：包络/幅值对环绕性的影响 |

> ★ **回归测试自身也曾有两处错误**（已修，值得记下）：
> 1. 包络写成了 **τ 的函数** `σ(τ/B)·σ((tEnd-τ)/B)`，而真实代码是
>    **真实时间**的函数 `blend_env(t - T1, T2, B)`；
> 2. 环绕段多加了 `startPosition` 水平偏移 (+0.10, −0.06)，
>    而真实代码是 `p = [p̂x·env, p̂y·env, zC]`，**以原点为中心**。
> 这两处让重建轨迹与代码不符、净间隙数字偏小（98.1 mm 而不是 142.3 mm）。
> 修正后与镜像吻合。**教训：任何"重建轨迹"的验证脚本都必须逐式对照源码，
> 不能凭记忆写。**

---

## 12. 故障排查：`RCOND = NaN` + `xlim` 报错（MATLAB 侧发散）

### 12.1 症状

```text
警告: 矩阵为奇异值、接近奇异值或缩放错误。结果可能不准确。RCOND = NaN。
> 位置：crazyflie_slung_dynamics (第 149 行)      ← Mblock \ rhsBlock
位置: crazyflie_slung_simulation (第 184 行)      ← crazyflie_slung_dynamics(...)
错误使用 xlim
范围必须为包含递增的数值的 2 元素向量。
出错 crazyflie_slung_visualization>plotSummary (第 67 行)
```

### 12.2 这两条报错是什么关系

- `RCOND = NaN` 是 **`\` 在矩阵含 NaN 时**发出的警告。所以**不是"矩阵本来病态"**，
  而是**状态里已经进了 NaN ⇒ 闭环已经发散**，发散之后才连带把代数求解搞坏。
- `xlim` 报错是**发散的下游后果**：`plotSummary` 用"稳态窗口内四旋翼集群质心 ±
  半跨度"定轴框，数据含 NaN 时算出 NaN，`xlim([NaN NaN])` 就抛错，
  于是**整个结果展示中断，用户连指标都看不到**。

### 12.3 已经做的三处加固（都是"让故障可读/更正确"，不是"把故障藏起来"）

1. **`crazyflie_slung_visualization.m`：轴框对 NaN 免疫。**
   轴框计算改为只统计**有限样本**；稳态窗口全非有限时退回**整段的有限样本**；
   连整段都没有有限样本才退回默认框并给出 warning。
   ⇒ 发散时你仍能看到指标打印与曲线（曲线上的**断点就是发散时刻**），
   而不是一句 `xlim` 报错。
2. **`crazyflie_slung_dynamics.m`：抑制刷屏。**
   6×6 求解前检查有限性；一旦发现非有限就**只警告一次**并跳过本步求解（解置零）。
   ⇒ 不再 26000 步反复打印 `RCOND = NaN`，真正有用的信息不会被淹没。
   ⚠ 这只是让故障可读，**不是把故障修好**。
3. **`crazyflie_slung_demo.m`：配置打印改为按挂点几何给。**
   原打印"单机悬停推力 = m0 g/n + mg = 0.5804 N"在新挂点下是**错的**
   （各机不同），会掩盖机 1 的 53.3% 推力需求。现改为打印几何解
   `[0.7112, 0.5150, 0.5150] N` 与最大占比，并打印完整 3×3 挂点矩阵与质心偏移。

### 12.4 ★ 诊断脚本 `crazyflie_slung_diagnose.m`

不画图，一次给出三样东西：

| 段 | 内容 | 用途 |
|---|---|---|
| [1] | **实际生效的关键参数**（挂点矩阵、`kR`、`rateLoop.bandwidth`、`amplitudeY`/`cruiseDuration`/`blendTime`、`initial.thrustNewton`、障碍物位置、`kx/kv/ki/kq`） | 排除"MATLAB 路径上其实是旧版参数文件" |
| [2] | 无绘图跑完仿真 | 用现成日志做分析 |
| [3] | **第一个非有限步的 k / t**，以及**每一条日志各自的首次失败步** | 判断哪一路先炸 |
| [4][5] | 故障**前一步**与**当步**的各通道数值（负载位置/角速度、绳向 q、绳向角速率、机体角速度与指令、推力与占比、绳索张力） | 定位机理 |

用法：

```matlab
>> cd('...\v3')                 % 进到本工程目录
>> crazyflie_slung_diagnose
```

### 12.5 截至目前的排查结论（诚实记录）

**Python 镜像用同一套参数是全程稳定的**：

```text
起飞 18.5 mm | 环绕峰值 705.0 / 均值 43.3 mm | 降落峰值 183.9 mm
降落终点残差 90.1 mm | 锥净间隙 125.3 mm PASS | 稳态张力 [0.3924 0.1963 0.1964] N
最小张力裕度 33.0% | 推力峰值 69.1% | max|Ω| 3.938 rad/s | 状态有限
```

**已逐项核对、确认 MATLAB 与 Python 两边一致的部分**（因此都被排除）：
参考轨迹 `crazyflie_slung_reference.m`、张力分配 `P'(PP')⁻¹`（含 `order='F'`）、
机体姿态环 `−kR·eR − kOmega·Ω`、速率环 `Ω̇ = BW·(ω_cmd − Ω)`、
积分顺序（半隐式欧拉 + `expSO3`）、`expSO3`/`projectSO3` 的小角保护、
全部控制器常数、初始条件（位置/姿态/绳倾角/机姿态）、`forceNormEpsilon`、
速率限幅 `maxBodyRate/maxPendulumRate`、`dt = 0.002`。
也确认工程内**没有同名 .m 文件**（不存在路径抢占）。

**镜像对参数的敏感度测试——极其稳健**（全部以"新挂点 + 当前参数"为基准）：

| 扰动 | 环绕峰值 | 结果 |
|---|---|---|
| 基准 | 705.0 mm | 稳定 |
| 初始推力 +5% | 705.0 mm | 稳定 |
| `kR` ×5 | 704.9 mm | 稳定 |
| `kR` ×8（采用） | 705.0 mm | 稳定 |
| `kR` ×12 | 705.1 mm | 稳定 |
| 速率环带宽 ×2 | 705.0 mm | 稳定 |
| 速率环带宽 ÷2 | 705.1 mm | 稳定 |
| `kq` 55 → 60 | 705.7 mm | 稳定 |
| `kq` 55 → 70 | 706.8 mm | 稳定 |
| `kx` ×0.5 | 1079.0 mm | 稳定 |
| `kx` ×1.2 | 613.7 mm | 稳定 |

**11 组扰动全部稳定**，性能只在 `kx` 上单调变化（符合预期），其余几乎不变。
kR 从 5× 到 12× 结果一模一样，说明该工况下姿态指令长期处在
`maxBodyRateCommand = 5 rad/s` 的**饱和平台**上 —— 闭环行为对增益不敏感。
这既解释了 §11.2 里"×5~×20 是平台区"，也说明**镜像里这套参数有很宽的稳定裕度，
不是卡在刀刃上** —— 因此 MATLAB 侧的发散**不太可能**只是"参数太临界"造成的。

**对照实验**：把挂点改成新的、`kR` 退回旧值 `[30;30;15]`，则镜像 **1.746 s 发散**；
此时 `kq=110`、`komega=40`、`KV×2`、速率环带宽 ×2/×4/×10 **全都救不回来**
（1.5~9.3 s 散开），**只有提高 `kR` 有效**。

> ★ **本节未解决。** 我不能调用本机 MATLAB，无法复现该发散。
> 请运行 `crazyflie_slung_diagnose` 并回贴输出：
> - 若 **[1] 段参数与本节不一致** ⇒ 是**文件版本**问题（MATLAB 路径上是旧参数）；
> - 若 **[1] 段一致** ⇒ 是真实数值问题，**[3]~[5] 段会指出哪一路先炸**，
>   据此再定修法（优先考虑负载/绳向环，而不是继续加增益）。

### 12.6 ★★ 诊断结果：参数正确、"日志全有限"是**假象**

用户跑 `crazyflie_slung_diagnose` 的输出否掉了两个可能性，同时暴露了**我上一轮引入的两个缺陷**：

**① `[1]` 段参数全部正确** —— 挂点矩阵、`kR=[240;240;120]`、`cruiseDuration=45`、
`blendTime=5.00`、`duration=52`、`initial.thrustNewton=[0.7112,0.5150,0.5150]`
全部与设计一致。⇒ **不是"MATLAB 路径上是旧版参数文件"**。

**② `[3]` 段"全程所有日志都是有限值"是假象。** 两次运行结果不一致
（第一次 25.2 s + 出现我的警告，第二次 12.4 s + 无警告），根因是
**我给动力学加的 `persistent` 标志在同一 MATLAB 会话内不会自动重置** ——
第二次运行守卫照样触发，只是警告被静默了。
⇒ 已修：`crazyflie_slung_diagnose` 现在会先 `clear('crazyflie_slung_dynamics')`。

**③ 更严重的问题：那个"发散保护"本身在掩盖故障。**
`Mblock`/`rhsBlock` 确实会出现 NaN/Inf；我的守卫把解置零，
于是 NaN **不再传进状态** —— 状态日志自然全程有限，
`summary.finiteState` 也照样通过。**用"日志里有没有 NaN"永远查不出来。**

**④ 顺带发现一个真实的观测盲区**：`state.loadVelocity` 参与积分、
也被动力学使用（`derivative.loadPosition = v0`），却**从未写入任何日志** ——
所以"逐日志查 NaN"根本看不到它。已补 `sim.loadVelocityLog`。

### 12.7 这一轮的四项加固

| # | 改动 | 作用 |
|---|---|---|
| 1 | `crazyflie_slung_dynamics.m` 增加**一次性详细报告**：触发时逐项打印 `rhs1/rhs2/Mq/sumMRho/sumRhoQ/sumRhoQRho/Omega0/R0/Qall/QDall/omegaAll/uAll/v0/hatOm0Sq` 的有限性，并完整 dump `Mblock` 与 `rhsBlock` | **下一次运行即可直接看到病因** |
| 2 | 动力学增加第二个输出 `info.nonFiniteInputs`；仿真累计成 `summary.nonFiniteSolveCount` | 得到**唯一不会骗人的判据**（坏步计数） |
| 3 | 补 `sim.loadVelocityLog`；诊断脚本把它纳入日志扫描 | 消除观测盲区 |
| 4 | **demo 自检新增一项**「代数系统无非有限量（发散保护未触发）」= `nonFiniteSolveCount == 0` | 让"发散被掩盖成通过"这件事**在自检里直接 FAIL** |

判据也顺手改成 `isnan`/`isinf`（base MATLAB 一定有），不再依赖 `isfinite`
（不确定它是否在 base MATLAB 还是工具箱里）。

> ★ 这次改动还**验证了静态检查的价值**：我在 `computeSummary` 里直接写了
> `summary.nonFiniteSolveCount = nonFiniteStepCount;`，而 `computeSummary`
> 是**另一个函数**、看不到主函数工作区的变量 —— lint 第 5 类
> （先用后定义）当场抓出来。已改为先 `sim.nonFiniteSolveCount = ...` 再传入。

### 12.8 ★★ 一条通用的排障教训

> **"状态日志里全是有限值"不能证明没有发散**，只要存在一层"把坏值置零"的保护，
> 日志就会被洗白。**必须另立一个不会被洗白的计数器**（这里是
> `nonFiniteSolveCount`），并且自检要断言它。
> 同源问题：**任何状态量都必须进日志**。`loadVelocity` 参与积分却不在日志里，
> 使得"遍历所有日志查 NaN"这种全局自检天然有盲区。

### 12.9 ★★★ 关键数据：坏步 20812 / 26001（80%）

用户第二次跑 `crazyflie_slung_diagnose` 得到：

```text
❌ 坏步计数 = 20812 步！代数系统曾出现非有限量（见上方一次性报告）。
日志有限性：全程有限。
```

**这彻底改变了问题的性质**：不是"跑到某个时刻开始发散"（那样坏步会是一段后缀），
而是**全程 80% 的步都命中**，即**系统性地**出现非有限量。

同时暴露了我上一轮的一个低级错误：
**`warning` 被写在了"只报一次"分支的外面** —— 于是每个坏步都刷一条，
2 万条警告把真正的病因报告彻底冲掉。**已修**（`warning` 移入 `if ~reportedNonFinite` 内）。

### 12.10 本轮改动

| 改动 | 说明 |
|---|---|
| `warning` 移入"只报一次"分支 | 不再刷屏；现在最多 1 条警告 |
| 报告**同时写入文件** `<tempdir>/cf_slung_nonfinite_report.txt` | 坏步上万时命令行输出必然被冲掉，写文件才能取回 |
| 报告新增**调用序号**（"首次出现在第 N 次动力学调用"） | 可直接换算成时刻 t ≈ N·dt |
| 报告新增 `size`/`class`、`nnz(NaN)`/`nnz(Inf)` 拆分、`rhoAll`、`J0`、`m0/m/l/g`、`uAll` 全量 dump | 覆盖 Mblock/rhsBlock 的**全部**输入来源 |
| 判据改为先算 `nnz(isnan)+nnz(isinf)` 再比较 | 比 `all(~isnan(...))` 更直白，且能报出个数 |

**Mblock/rhsBlock 的全部输入来源（这是排查的完整清单）**：

```text
状态量（都进日志）: Qall(q_i)、QDall(q̇_i)、R0、Omega0
非状态输入        : uAll（实际作用力，= -f_act·R_i·e3，由 thrustLog × rotationLog 决定）
常量              : m0、m、l、g、J0、rhoAll
```

如果下一份报告里这些**全都是 `[ok]`**，那就说明问题在**判据之外的路径**
（例如 `Mblock` 本身规模/类型异常），报告里已一并打印 `size`/`class` 与原始 dump。

### 12.11 ★★★ 真实因果链（2026-09-21 定位）

用户第三份诊断报告带来了决定性证据。**`uAll`（实际作用力）全是 0**，
`R0` 第三列 = (−0.822, −0.568, 0.037)（负载"上表面"几乎水平 ⇒ **翻了约 88°**），
`Ω₀` = 1e289，`hatOm0Sq` 9/9 全 NaN，`rhs1/rhs2` 全 NaN，而 **`Mblock` 完全正常**。

**我用用户 dump 的矩阵实算了条件数**：

```text
cond(Mblock) = 7.64e+02   rcond = 1.31e-03   norm(inv) = 4.79e+03
奇异值 = [0.1595 0.0945 0.0837 0.0003 0.0002 0.0002]
最弱方向右奇异向量以 Omega0_dot 的 x/y 分量为主 (0.660, 0.668)
rhs 扰动 1e-16 -> |dOmega0_dot| = 4e-13     ← 放大到 1e289 是不可能的
```

⇒ **"矩阵病态"被彻底排除。**

**真实链条（每一步都可由上面的 dump 验证）**：

```text
① 控制器某一拍输出 NaN  ⇒ command.thrustDesired = NaN
② actualThrust = min(max(NaN,0), maxTotalThrust)
   ★★ MATLAB 的 max/min **会忽略 NaN**，于是 NaN 被洗成 0  ⇒ actualThrust ≡ 0
③ ⇒ uActual ≡ 0  ⇒ 四旋翼完全不出力
④ ⇒ 负载失稳翻转（R0 显示翻了 ~88°）
⑤ ⇒ 负载角速度爆到 1e289
⑥ ⇒ hat(Omega0)^2 平方溢出成 Inf，Inf−Inf 得 NaN ⇒ rhsBlock 全 NaN
⑦ ⇒ 发散保护触发（20812 个坏步），解被置零 ⇒ **状态日志被洗白**
```

> ★★ **两个重要教训**
> 1. 我们一开始追的 `RCOND = NaN` 只是第 ⑥ 步的**次生现象**，
>    真正的起点在第 ①~② 步。**一路盯着"矩阵/日志"看，方向从一开始就错了。**
> 2. **`uActual ≡ 0` 能通过所有"有限性"自检**（0 是有限的）。
>    ⇒ "只查 NaN/Inf"是不够的，**还要查"该非零的量是否变成了 0"**。
>    这是本项目第三次遇到"判据选错就看不见"（前两次：`gap=inf` 静默失效、
>    "日志全有限"被置零保护洗白）。

### 12.12 本轮新增：发散**起点**探测

在 `crazyflie_slung_simulation.m` 的主循环里加了一次性起点探测，每步自查四件事，
**第一次命中就打印那一拍的完整上下文**，并记下 `summary.onsetStep`：

| 优先 | 判据 | 含义 |
|---|---|---|
| ① | `~all(isfinite(command.thrustDesired))` | NaN 起点在**控制器** |
| ② | `~all(isfinite(command.desiredForce))` 或 `totalForces` | 起点在力/力矩合成 |
| ③ | `norm(state.loadBodyRate) > 1e5` | 6×6 解已给出异常大的 `Omega0_dot` |
| ④ | `max(abs(actualThrust)) == 0` | 指令已被 `min/max` 洗成 0 |

打印内容：步号与时刻、`Omega0`、**上一拍 `Omega0_dot`**、负载位置、
`thrustDesired`、`actualThrust`、`Fd`、`Md`、`u_i`、`mu_id`、张力、角速度指令、`q_i`。

同时修正了上一版报告里的一个**错误标签**：动力学里的计数 `nDynCall` 只在坏步自增，
却被标成"第 N 次动力学调用"——应为"**第 N 个坏步**"。已改。

### 12.13 起点探测的第一次结果：阈值设得太松，漏掉了真正的起点

2026-09-21 用户运行的输出：

```text
*** 发散起点定位：k = 5183, t = 10.3640 s ***
    现象：负载角速度 ||Omega0|| > 1e5
    负载角速度 Omega0      = [-647238, 897212, -1.32248e+06]
    上一拍 Omega0_dot      = [-3.11e+08, 4.56e+08, -6.48e+08]
    负载位置               = [-144.835, 16.917, 15.143]     ← 已飞到 145 m 外
    实际推力 actualThrust  = [1.3344, 0, 0]                 ← 机1 饱和、机2/3 为 0
    期望推力 thrustDesired = [1.64e+09, -2.47e+09, -2.83e+09]
    绳向 q_i 的 z 分量     = [-0.558, +0.866, +0.555]       ← 几何已翻转
```

**两个关键推断**：

1. **`Ω₀(5183) = Ω₀(5182) + dt·(-3.11e+08, …)`，而 `Ω₀(5183) ≈ 1e6`
   ⇒ `Ω₀(5182) ≈ 0`。** ⇒ 这一步是**瞬间爆炸**，是**结果**不是起点。
2. **但负载在爆炸前就已经在 145 m 外了** ⇒ 真正的发散发生在更早。
   而 ①（控制器 NaN）与 ④（推力全 0）都没触发 ⇒ **早期是"平移跑掉"，
   不是控制器出 NaN，也不是推力死掉。** 我设的 `||Ω₀|| > 1e5` 阈值**太松**。

> ★ **教训：阈值型探测一定会漏。** 拐点必须用**时间历程**看，
> 而不是靠猜一个阈值。已改为"时间历程抽样"为主、阈值为辅。

### 12.14 新增：时间历程抽样（无阈值证据）+ 收紧阈值

`crazyflie_slung_diagnose` 现在会打印 **[3.5] 时间历程抽样**：全程粗扫 36 点 +
前 30% 细扫 36 点，每行给出 `k / t / |e| / ||Ω₀|| / maxThr% / max|T|`。

**镜像（Python）的同一张表（作为对照基准）**：

| k | t(s) | \|e\| (m) | \|Ω₀\| (rad/s) | maxThr% | max\|T\|(N) |
|---|---|---|---|---|---|
| 0 | 0 | 0 | 0 | 69.1 | 0.452 |
| 2000 | 4 | 5.71e-3 | 1.71 | 53.4 | 0.394 |
| 4000 | 8 | 1.26e-2 | 2.60 | 53.4 | 0.392 |
| 6000 | 12 | 1.90e-3 | 0.57 | 53.3 | 0.392 |
| 10000 | 20 | 3.97e-3 | 1.53 | 53.3 | 0.393 |
| 20000 | 40 | 8.75e-4 | 0.43 | 53.3 | 0.392 |
| 26000 | 52 | 9.01e-2 | 0.86 | 53.3 | 0.392 |

镜像全程 **max\|e\| = 0.705 m、max\|Ω₀\| = 4.06 rad/s、maxThr = 69.1%**，
从未超过 1 m ⇒ **只要 MATLAB 那张表前几行出现数量级差异，就能定位起点**。

起点探测的判据同时收紧（原来 `\|\|Ω₀\|\| > 1e5` 太松）：

| 优先 | 判据 | 含义 |
|---|---|---|
| ① | `thrustDesired` 非有限 | NaN 起点在控制器 |
| ② | `desiredForce` / `totalForces` 非有限 | 起点在力/力矩合成 |
| ③ | `max\|actualThrust\| == 0` | 推力被 `min/max` 洗成 0 |
| ④ | **`\|\|Ω₀\|\| > 5`**（原 1e5） | 远超正常值（镜像 max 仅 0.86） |
| ⑤ | **`\|z\| > 3 m` 或 `\|\|(x,y)\|\| > 5 m`** | **平移跑出工作区** ← 本应最早命中这条 |

### 12.15 ★★★ 时间历程一次定案：是**偏航转速失控**，不是平移失稳

2026-09-21 第二次带时间历程的输出，细扫表（节选）：

| k | t(s) | **\|e\| (m)** | **\|Ω₀\|** | maxThr% | max\|T\|(N) |
|---|---|---|---|---|---|
| 3121 | 6.24 | 1.4e-2 | 2.77 | 53.4 | 0.393 |
| 3566 | 7.13 | 2.7e-3 | 2.24 | 53.3 | 0.390 |
| 3789 | 7.58 | 6.1e-3 | **4.16** | 53.8 | 0.393 |
| 4012 | 8.02 | 1.2e-2 | 3.95 | 53.4 | 0.388 |
| 4458 | 8.91 | 7.4e-3 | **5.22** | **56.1** | 0.389 |
| 4680 | 9.36 | 6.0e-3 | **7.71** | 57.8 | 0.392 |
| 4903 | 9.80 | 8.2e-3 | 7.46 | **67.3** | 0.405 |
| 5126 | 10.25 | **6.6e-2** | 5.91 | **100.0** | 0.453 |
| 5349 | 10.70 | 3.9e+287 | 3.6e+289 | 0.0 | 5.8e+287 |

**⇒ 关键读法：`|e|` 全程只有毫米~厘米级（跟踪**完美**），
但 `||Ω₀||` 在**单调爬升**；推力占比随之从 53.4% 抬到 56→58→67→**100%（饱和）**，
之后才是负载跑掉与最终爆炸。**

起点那一拍（k = 4044, t = 8.086 s）的 dump 把方向钉死了：

```text
Omega0        = [-0.000948088, 0.0233693, -5.01035]     ← x,y≈0，只有 z 分量爆了
Omega0_dot    = [ 0.166136,   0.0851238, -14.4254]
Md            = [-2.36e-05,   3.47e-05,   0]            ← Md(3) ≡ 0（偏航通道关闭）
actualThrust  = [0.712572, 0.525597, 0.526186]          ← 正好是 2:1:1 悬停值
Fd            = [-0.00661105, 0.013254, -0.780465]      ← 与 m0·g 一致
q_i 的 z 分量 = [0.99475, 0.99046, 0.98931]             ← 三根绳都近乎竖直
负载位置      = [0.433379, 0.688185, -0.551813]
参考位置(τ=4.52) ≈ [0.4395, 0.6849, -0.55]              ← 位置误差仅 7 mm！
```

**⇒ 一切"正常"——位置跟踪 7 mm、推力正好 2:1:1、绳全竖直、车姿态指令未饱和——
唯独负载在绕**自身偏航轴**以 5 rad/s 狂转（且以 −14.4 rad/s² 在加速）。
而偏航通道的 `KR0(3) = KO0(3) = 0`，`Md(3) ≡ 0` ⇒ **没有任何东西在阻尼这个转动**。**

**机理**：负载偏航 ⇒ 挂点随负载旋转 ⇒ 近乎竖直的绳索被"拧"出切向分量 ⇒
产生绕负载质心的偏航力矩 ⇒ 由于偏航通道关闭、`q_id` 里也不含偏航修正，
**绳向环无法把该力矩交还出去** ⇒ 偏航角速度持续累积 ⇒ 推力逐渐饱和 ⇒ 崩溃。

**与旧挂点的区别**：新挂点把挂点质心偏了 **0.0333 m**，偏航与重力/绳向的耦合显著变强
（旧的内接等边三角形质心与负载质心重合，耦合弱得多）——
所以"偏航可以不控"这个旧结论**在新几何下不再成立**。

**镜像的对照**：镜像偏航率终值 0.862 rad/s、`||Ω₀||` 峰值 4.06 rad/s —— 同样是偏航主导，
但**有界**；MATLAB 则 4.16 → 5.22 → 7.71 持续增长。
⇒ 两者机制相同，差别在于**是否有足够的偏航耗散**。

### 12.16 ★★★ 最终修复：把**物理转动阻尼**补进模型

**关键判断**：偏航反馈这条路是死的 —— 实测 `KR0(3)`/`KO0(3)` 只要非零就发散
（连 `KO0(3) = 0.005` 这种纯阻尼都炸到 1e8）。机理是 `Md(3)` 会经
"张力分配 → `mu_id` → `q_id` 水平化"形成正反馈（见 §5.1）。

**⇒ 唯一可行的路：把阻尼做成****外部力矩**，加在负载转动方程右端，
**不经控制器、不进张力分配**：

```matlab
dampingTorque = -cfg.payload.rotationalDamping * Omega0;
rhsBlock = [rhs1; rhs2 - cross(Omega0, J0 * Omega0) + dampingTorque];
```

**取值依据**（物理量级估算，不是凑参）：边长 `a = 0.20 m` 的方板绕法向以 `ω` 旋转，
气动阻尼力矩约 `(1/8)ρ C_d a⁴ ω²`；取 `ρ = 1.225`、`C_d ≈ 1.1`、`ω = 3 rad/s`
得 ≈ `2.4e-3 N·m` ⇒ 等效线性系数 `c ≈ 8e-4`。**取 `c = 1.0e-3 N·m·s/rad`。**

**c 的有效区间很宽**（镜像实测）：

| c | max\|Ω₀_z\| | \|e\|峰 | maxThr% | 结果 |
|---|---|---|---|---|
| 0 | **4.058** | 705.0 mm | 69.1 | 稳定（但偏航剧烈振荡） |
| 5e-4 | 0.336 | 705.1 mm | 69.1 | 稳定 |
| **1e-3（采用）** | **0.19** | **705.1 mm** | **69.1** | **稳定** |
| 2e-3 | 0.144 | 705.1 mm | 69.1 | 稳定 |
| 1e-2 | 0.051 | 705.1 mm | 69.7 | 稳定 |
| 5e-2 | 3.1e7 | 1.2e7 mm | 100 | 发散（过阻尼 + 离散化） |

### 12.17 修复后的完整结果（c = 1e-3）

| 指标 | 修复前 | **修复后** |
|---|---|---|
| **偏航漂移率** | 0.862 rad/s | **0.001 rad/s**（↓ 862 倍） |
| **负载角速度终值** | `[0, 0, 0.862]` | **`[0, 0.0001, -0.0006]`** |
| `max‖Ω₀‖` | 4.058 rad/s | **0.19 rad/s**（↓ 21 倍） |
| 稳态绳向误差 | `[0.577, 1.183, 1.05]°` | **`[0.316, 0.696, 0.638]°`**（更好） |
| 稳态机体姿态误差 | `[0.145, 0.272, 0.27]°` | **`[0.027, 0.036, 0.034]°`**（更好） |
| 环绕峰值 / 均值 | 705.0 / 43.3 mm | 705.1 / **43.3** mm（不变） |
| 降落终点残差 | 90.1 mm | **90.0** mm（不变） |
| 稳态张力 | [0.3924, 0.1963, 0.1964] N | [0.3928, 0.1962, 0.1962] N（=理论） |
| 推力峰值 / 净间隙 / 绳长不变量 | 69.1% / 125.3 mm / 2.22e-16 | 完全相同 |

**⇒ 是严格改进，不是取舍**：偏航通道从"剧烈振荡"变成"基本静止"，
位置跟踪与所有安全裕度**一个都没变差**，绳向误差与机体姿态误差还**明显变好**。

> ★ **必须如实说明**：这是**模型补充**，不是控制器修改。
> 论文假定负载是无阻尼刚体；真实负载总有转动阻尼（气动/摩擦/绳索内摩擦）。
> 用户要求的挂点几何**必然**带来 0.0333 m 的质心偏移，
> 由此产生的偏航模态在"无阻尼 + 不可控"下恰好骑在稳定边界上 ——
> 旧挂点（质心与负载质心重合）没有这个模态，所以旧的"偏航可以不控"结论
> **在新几何下不成立**。补上阻尼既恢复了物理真实性，也把该模态推回稳定区。

### 12.18 ★ 一处必须更正的旧结论

我之前报告过"镜像偏航漂移率 0.862 rad/s、判据 1.0、裕度仅 14%"。**这个说法是错的** ——
镜像的偏航并不是缓慢漂移，而是 **±3.5 rad/s 的持续振荡**
（t=4 s 时 +1.71、t=6 s 时 −3.44、t=8 s 时 +2.60、t=10 s 时 −3.50），
只是**恰好不发散**。只看"终值/漂移率"这类单点指标会把振荡误读成漂移 ——
**必须看时间历程**（这次就是靠它定的案）。

### 12.19 本轮落地清单

| 文件 | 改动 |
|---|---|
| `crazyflie_slung_parameters.m` | 新增 `cfg.payload.rotationalDamping = 1.0e-3`（含完整依据与失败记录） |
| `crazyflie_slung_dynamics.m` | `rhsBlock` 右端加 `- cfg.payload.rotationalDamping * Omega0`；起点报告同步加强 |
| `_verify_python/_multi_mirror.py` | `ROT_DAMP = 1.0e-3`，同位置加 `- ROT_DAMP * Om0` |
| `crazyflie_slung_diagnose.m` | 时间历程表**新增 `Om0_z` 列**（只看 `‖Ω₀‖` 会被 x/y 的小量稀释）；起点判据收紧到 `‖Ω₀‖>5` 并补上"跑出工作区" |
| `crazyflie_slung_simulation.m` | 发散**起点探测**（一次性，打印该拍全部上下文 + 记入 `summary.onsetStep`）；补 `loadVelocityLog`；`summary.nonFiniteSolveCount` |

---

## 13. 挂点在图上看起来"不在边缘"—— 是**绘制缩放**的锅，几何一直是对的

### 13.1 症状与误判

用户看图后指出"挂点不在物品边缘"。**几何本身完全正确**，数值核验：

```text
负载 0.20 x 0.20 x 0.020 m  ⇒  半宽 (hx, hy, hz) = (0.100, 0.100, 0.010)

挂点   坐标                     到最近竖直边(m)  到上表面(m)   判定
 1   (+0.100, +0.000, -0.010)      0.00000       0.00000     在边缘 ✓
 2   (-0.100, +0.100, -0.010)      0.00000       0.00000     在边缘 ✓
 3   (-0.100, -0.100, -0.010)      0.00000       0.00000     在边缘 ✓
```

挂点 1 是 `x = +0.10` 那条边的**中点**；挂点 2、3 是**对边** `x = -0.10` 的两个**顶点** ——
与用户要求逐字对应。

### 13.2 真正的原因：`payloadSizeScale = 2.0`

```matlab
[payloadVert, ~] = boxVerticesFaces(cfg.payload.size * cfg.visualization.payloadSizeScale);
...
set(payloadPatch, 'Vertices', (RlD * payloadVert + pl).');      % 负载按 2 倍画
set(attachPointX, ..., RlD * cfg.payload.attachPoints(:, i) + pl);  % 挂点按**真实**位置画
```

**负载被放大 2 倍显示（画成 0.40 m 的板），而挂点/绳索仍在真实位置。**
于是挂点落在"板面 1/(2×2) = 25% 处"，**看起来就像挂在板子中间**。

⚠ 更要紧的是：四旋翼位置由 `veh = x0 + R0·rho_i − l·q_i` 决定，绳索连到**真实**挂点 ——
所以**不能**靠"把挂点也一起放大"来对齐画面，那会让绳索与四旋翼脱节。
唯一正确的做法是**让负载按真实尺寸画**。

### 13.3 修复（三处，形成闭环防线）

| # | 改动 | 作用 |
|---|---|---|
| 1 | `payloadSizeScale` **2.0 → 1.0** | 几何保真：画的板边 = 真实板边 = 挂点位置 |
| 2 | 可视化里新增 **`PayloadScaleNotOne` 警告**：一旦 `payloadSizeScale ≠ 1`，明确提示"负载被放大显示、挂点会落在板面内部，看起来像挂在中间" | 防止后人再犯同样的误判 |
| 3 | demo 配置区新增**逐点边界距离打印** + 自检项「三个挂点都在物品边缘（到边界距离 = 0）」 | 用**数字**而不是图来确认，谁也误导不了 |

> ★★ **通用教训**：**"纯显示"的缩放参数也会让人对物理布置得出错误结论。**
> 凡是与几何布置有关的量（挂点、机臂、绳索端点），显示缩放要么统一乘进去、
> 要么就保持 1:1；**绝不能只缩一部分**。而更稳妥的规则是：
> **对几何位置这类"会被用来判断对错"的量，必须同时给出数值自检，
> 不能只依赖人眼看图。**

### 13.4 ★★ 我自己引入的语法错误：注释插进了 `...` 续行块

改 `payloadSizeScale` 时，我把 7 行注释**插进了 `struct(...)` 的续行块中间**：

```matlab
cfg.visualization = struct(...
    'videoFile', 'crazyflie_slung_multi_demo.mp4', ...
    % ★★★ 负载显示缩放必须为 1.0 —— ...
    %   挂点画在**真实**位置 ...
    'payloadSizeScale', 1.0, ...
```

MATLAB 直接报：

```text
文件: crazyflie_slung_parameters.m 行: 549 列: 48
无效表达式。调用函数或对变量进行索引时，请使用圆括号。否则，请检查不匹配的分隔符。
```

**MATLAB 规则：`...` 续行块里每一行（\*\*包括注释行\*\*）都必须以 `...` 结尾。**
注释行不以 `...` 结尾会把语句提前截断，后面的 `'payloadSizeScale', 1.0, ...`
就变成一条孤立的新语句 ⇒ "无效表达式"。

**修法**：把注释块整段移到 `struct(...)` **外面**（已在本文件里加了警示注释说明原因）。

### 13.5 ★★ 静态检查的第 7 类：续行块语法

**这个错误是"硬语法错误"，却通过了原来的 6 类检查** —— 因为前 6 类
（可见性/使用顺序/nargout 分派/cfg 路径/字段/配平）**都不做续行语法分析**。

⇒ 已新增 **第 7 类：续行块语法检查**：若某行以 `...` 结尾，
而**下一行是不以 `...` 结尾的注释行**，即判为"语句被注释截断"。
判据构造上不会误报（前一行以 `...` 结尾就意味着语句一定在继续）。

同时：
- 新增 `_strip_strings()` 助手，先把 MATLAB 字符串字面量抹成空格，
  避免把 `'a...b'` 这种字符串里的 `...` 误判成续行符；
- 已固化进 `_test_lint_regress.py` 的第 ③ 组回归用例：
  **坏样例必须被抓到、两种好样例不得误报、字符串里的 `...` 不得误判、
  8 个交付文件必须全干净**。

现在检查项：`可见性 | 使用顺序 | 分派 | cfg路径 | 字段 | 配平 | 续行` 共 **7 类**。

> ★★ **教训**：**"静态检查通过"不等于"语法正确"。**
> 前 6 类检查全是**语义层**的（谁定义了、谁可见、参数对不对），
> 而这次栽在**语法层**。凡是"文本级"的检查，都必须显式覆盖
> **续行、字符串字面量、注释**这三个容易互相干扰的语法要素。
> 更根本的一条：**改完 .m 必须在 MATLAB 里至少跑一次**
> —— 因为本环境不能调用 MATLAB，所以语法层只能靠这类文本检查兜底。

---

## 14. 期望偏航改为**路径切线方向**（论文原式，用户要求）

### 14.1 改了什么

`cfg.figureEight.lockYaw`：**true → false**。
负载的期望姿态第一轴从"恒定 +x（期望偏航 = 0）"改为**沿路径切线方向**，
即论文原式的 `R0d = [ẋ_d/‖ẋ_d‖, ê₃×ẋ_d/‖ê₃×ẋ_d‖, ê₃]`，**偏航随路径转**。

### 14.2 ★★★ 关键：切线必须取"理想八字的解析切向"，不能取"实际速度方向"

**这是本次改动能成立的核心。** 直接用实际速度方向会踩两个坑：

1. **方向退化**：环绕段 `v = env'·p̂ + env·s·v̂`，在两端 `env→0` 且 `env'→0`
   ⇒ `|v|→0` ⇒ `atan2` 的**方向无定义**。起飞/降落段同样有水平速度过零点。
2. **拼接不连续**：起飞段速度方向 ≈ 149°（从 `(0.10,−0.06)` 归位到原点），
   与环绕段端点切向 0° 不一致。

**改用理想八字（不含包络）的解析切向**：

```text
u(τ) = aX·wX·cos(wX τ)      v(τ) = (aY/2)·wY·sin(wY τ)
ψ    = atan2(v, u)
ψ̇    = (u·v' − v·u')/(u²+v²) · s          ← 解析，链式法则 d/dt = s·d/dτ
```

- **两个分量不会同时为零**：`wX = 2 wY`，联立要求 `k = 2m − 1/2`，**无整数解**
  ⇒ 全程良定义；
- **两端恰为 +x**：`τ=0` 与 `τ=tEnd` 处 `v=0`、`u>0` ⇒ `ψ = 0`，
  与起飞/降落段的 `ψ ≡ 0` **天然连续**（实测 `ψ(tEnd) = −1.3e-16 rad`）。

因 `b3d ≡ e3`，`R0d = Rz(ψ)` 是**绕世界 z 的纯偏航** ⇒
`Ω0d = [0; 0; ψ̇]`、`Ω0dDot = [0; 0; ψ̈]`（体系与世界系一致）。
`ψ̈` 只对**解析的** `ψ̇` 做**一次**中心差分 —— 而旧实现是对 `R0d` 做**二阶**差分，
噪声大一个量级（`R0d` 由速度方向构造、速度里又含包络导数项）。

**解析式已与数值微分逐点比对通过**（`ψ` 完全一致、`ψ̇` 误差 < 1e-4）：

| τ | 0 | 2 | 6.55 | 12 | 20 | 40 |
|---|---|---|---|---|---|---|
| ψ (°) | +0.0000 | +27.18 | +157.06 | −27.18 | −0.0000 | −0.0000 |
| ψ̇ (rad/s) | +0.0754 | +0.8547 | −0.2405 | −0.8547 | +0.0754 | +0.0754 |
| ψ̈ | +0.000 | +2.283 | −0.428 | −2.283 | +0.000 | +0.000 |

### 14.3 为什么以前不行、现在行了

早期实测"`lockYaw = false` 会发散"，当时有**两个叠加原因**，现在都已解决：

| # | 旧问题 | 现在 |
|---|---|---|
| ① | 负载"偏航 ↔ 绳索扭转"模态**完全没有阻尼**（新挂点必然带来 0.0333 m 质心偏移，产生 ω≈2.2 rad/s 的该模态，见 §12） | 已补物理转动阻尼 `rotationalDamping = 1e-3`（§12.16） |
| ② | `Ω0d` 用**对 `R0d` 二阶中心差分**，噪声大 | 改为**解析切向**（本节） |

### 14.4 ★★★ 诚实的实测结果：**参考**改成切线了，但**负载跟不上**

**改完之后的完整跟踪统计（这是正确读法）：**

| 量 | 值 |
|---|---|
| 环绕段**偏航跟踪误差**（负载实际偏航 − 参考切线） | **均值 85.8° / 中位 85.4° / 最大 174.9°** |
| 环绕段 `\|Ω₀_z\|` 峰值 | 4.03 rad/s（参考切线最大 `\|ψ̇\| ≈ 2.95`） |

**⇒ 负载的偏航根本没有跟着切线转 —— 误差基本是随机的。**

> ⚠ **我在本轮前半段犯过一个读数据的错误**：我只看 `summary` 里
> "稳态负载姿态误差 yaw = 1.900°" 就宣布成功。但那是 **t = 52 s 单个瞬间**的值，
> 而 `ψ_ref` 在终点恰好回到 0、负载偏航也恰好接近 0 ⇒ **两个"都接近 0"被误读成"跟上了"**。
> **判据必须用整段的误差统计，不能用单点终值。** 这与 §12.18 那次
> "只算漂移率、漏看振荡"是同一类错误 —— 已第二次踩到。

**为什么跟不上（结构性原因）**：

偏航通道的反馈增益是 **0**（`kR0(3) = kOmega0(3) = 0`，`Md(3) ≡ 0`），
参考偏航只能通过极小的前馈项（`hat(ff)·J0·ff + J0·R0'R0d·Ω0dDot`，量级 ~1e-3 N·m）进入控制器，
**几乎不影响动力学**。

**而把它打开（哪怕只给 0.0183 的纯阻尼）就发散 —— 且本轮是在"转动阻尼已就位"的前提下重测的**：

| `KR0(3)` | `KO0(3)` | `\|Ω₀\|`峰 | 偏航误差均值 | 结果 |
|---|---|---|---|---|
| 0 | 0 | 4.03 | 85.8° | 稳定（基准） |
| 0 | 0.0183 | 1.6e11 | 84.9° | **发散** |
| 0 | 0.05 | 2.0e11 | 84.9° | **发散** |
| 0.02 | 0.0183 | 6.5e7 | 84.9° | **发散** |
| 0.05 | 0.0183 | 8.5e10 | 84.9° | **发散** |
| 0.1 | 0.0183 | 1.2e9 | 84.9° | **发散** |
| 0.3828 | 0.0183 | 3.1e12 | 84.9° | **发散** |

⇒ **§12.16 的转动阻尼只解决了"无阻尼振荡"，并没有解锁偏航反馈。**

**机理（为什么在本构型下控制不了偏航）**：
负载的偏航力矩只能靠**把近乎竖直的绳索"扭出"水平分量**来交付。
需求 `Md(3)` 时，分配解 `μ_id` 必须给出水平分量 `\|μ_h\| ≈ \|Md(3)\|/(n·ρ)`；
本构型 `ρ ≈ 0.1 m`（0.2 m 负载）⇒ 力臂很小 ⇒ 需要**很大的水平张力** ⇒
`q_id` 被甩出大角度 ⇒ 与绳向环形成正反馈。
论文能控偏航，是因为它的负载是 1.0×0.8 m、绳长 1 m，**力臂大一个量级**。

### 14.4b 位置跟踪完全不受影响（这部分是真的）

| 指标 | 偏航锁定（旧） | 切线参考（新） |
|---|---|---|
| 环绕峰值 / 均值 | 705.1 / 43.3 mm | **704.9 / 43.4 mm** |
| 降落终点残差 | 90.0 mm | **90.0 mm** |
| 锥净间隙 | 125.3 mm | **125.3 mm** |
| 推力峰值 / `max‖Ω‖` | 69.1% / 3.949 | **69.1% / 3.949** |
| 稳态张力 | [0.3928, 0.1962, 0.1962] | [0.3926, 0.196, 0.1961]（=理论） |
| 绳长不变量 / 最小张力裕度 | 2.22e-16 / 33.0% | 2.22e-16 / 33.0% |
| 稳态绳向误差 | [0.316, 0.696, 0.638]° | [0.796, 1.32, 1.377]°（≪3°） |
| 稳态机体姿态误差 | [0.027, 0.036, 0.034]° | [0.689, 0.74, 0.737]°（≪8°） |
| **负载偏航** | 自由、缓慢漂移 | **自由（同样不跟踪参考）** |
| 状态有限 | true | **true** |

### 14.5 顺带清理

改为解析切向后，原 `lockYaw = false` 分支依赖的 6 个局部函数全部失效，已删除：

`yawReferenceAxis`、`rotationMatrixOnly`、`omegaOnly`、`referenceRaw`、
`yawReferenceAxisRaw`、`buildRotation`、`vee`
（`crazyflie_slung_reference.m` 由 399 行降到 296 行）。
新增 `yawFromIdealTangent` + `yawPsiAndRate` 两个助手。

### 14.6 ★ 镜像同步（顺带修掉一处两边不一致）

同步时发现：**镜像在 `lockYaw=false` 时把 `Ω0d` 直接返回 0**，
而 MATLAB 用 `R0d` 的中心差分 —— **两边行为不同**。
现在两边都用同一套解析式（逐式对齐），并新增
`_yaw_psi_and_rate` / `_yaw_from_ideal_tangent` 两个镜像函数。
